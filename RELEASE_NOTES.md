<!-- Rewrite this file for every release. GitHub appends the generated changelog below it. -->

## 版本亮点

- **URL 与参数表双向同步**：地址栏里的查询参数会自动回填到「参数」页签；编辑参数表也会同步重建 URL。沿用旧版录入的请求会在首次启动时自动补齐，发送时不再重复拼接同名参数。
- **请求变量悬停提示**：把鼠标悬停在 URL 中的 `{{变量}}` 上，可查看它的取值与定义来源（请求变量 / 环境 / 集合 / 全局）。
- **继承脚本只读保护**：从集合或文件夹继承来的前置 / 后置脚本在请求内以只读形式展示，并支持「解锁为副本」等修复动作，避免误改父级脚本。

## 下载与安装

- **Windows**：下载 `.exe` 安装包。
- **macOS**：下载 `.dmg` 安装包。
- **Chrome / Edge**：下载 `postino-extension-*.zip`，解压后通过浏览器的扩展管理页面加载。

升级前建议先在设置页创建完整数据备份。
