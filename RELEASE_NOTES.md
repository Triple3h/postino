<!-- Rewrite this file for every release. GitHub appends the generated changelog below it. -->

## 版本亮点

- 环境变量支持按集合保存不同的值，切换工作场景时更方便。
- 设置页新增完整数据备份和数据重置工具。
- 快速请求面板重新设计，新增请求方法选择器和取消操作。
- 请求完成后可以从响应中提取数据并写入变量。

## 下载与安装

- **Windows**：下载 `.exe` 安装包。
- **macOS**：下载 `.dmg` 安装包。
- **Chrome / Edge**：下载 `postino-extension-*.zip`，解压后通过浏览器的扩展管理页面加载。

升级前建议先在设置页创建完整数据备份。
