# design-doc.md 实现审计与打磨记录

> 目标：对照 `design-doc.md` 标注已实现/未实现设计项，并持续记录补齐过程。  
> 状态：进行中。每次补齐功能后更新本文件，作为上下文压缩/续接依据。  
> 上下文策略：已在上下文不足 50% 后压缩续接；后续继续以 `design-doc.md` + 本记录作为恢复依据。

## 本轮已打磨实现

| 时间 | 设计项 | 状态 | 代码证据 | 说明 |
|---|---|---|---|---|
| 本轮 | 只读模式禁止手动修改 | 已补齐 | `src/components/editor/RequestBar.vue`, `src/components/editor/TabPanel.vue`, `src/components/editor/BodyEditor.vue`, `src/components/editor/AuthConfig.vue`, `src/components/editor/CookieConfig.vue` | 原先只禁用 URL；现在 Params / Body / Headers / Cookies / Auth / 前后置脚本编辑入口均按 readonly 模块锁定，更新函数也二次拦截。 |
| 本轮 | 浏览器右键/DevTools 捕获后的待导入消费 | 已补齐 | `extension/background.js`, `extension/content.js`, `extension/devtools-panel.js`, `src/components/common/PendingImportBridge.vue`, `src/App.vue` | 原先扩展端已存 `apifix_pending_import`，应用端未消费；现在启动/消息更新时自动转成接口并放入“浏览器捕获/页面上下文”模块。 |
| 本轮 | Popup 闪电面板 | 已补齐基础能力 | `src/views/PopupView.vue` | 从单行快速请求扩展为 800×600 设计：最近模块卡片、快捷操作、JSON/Text Body、最近 5 条历史、底部状态/完整版入口；模块卡片点击会进入快速发送模式并预填首个请求或模块 baseUrl 变量；顶部搜索框已支持跨模块按接口名/URL/模块实时检索，回车发送第一条结果，并显示脚本徽标与底部模块/接口统计；响应区默认只显示状态/耗时/大小，按状态/耗时阈值着色且大小自动换算，Body 仅按折叠预览展示前 1000 字符；历史项可一键在 Side Panel 或全屏页复用，缺少本地接口时会写入 pending import 预填充请求。 |
| 本轮 | Popup 响应折叠预览与历史复用 | 已补齐基础能力 | `src/views/PopupView.vue` | 对齐设计文档“不展示完整响应 Body”：Popup 请求完成后默认仅展示状态码、耗时、大小，点击后只展开前 1000 字符预览；最近历史行新增“侧栏/全屏”动作，已存在接口走打开上下文，临时请求走 pending import 预填充。 |
| 本轮 | Popup 响应状态规则 | 已补齐基础能力 | `src/views/PopupView.vue` | Popup 响应摘要对齐响应状态栏规则：2xx 绿色、3xx 蓝色、4xx 橙色、5xx 红色，耗时超过 1s/3s 分别警告/错误色，响应大小自动换算 B/KB/MB。 |
| 本轮 | Popup 模块卡片快速发送预填充 | 已补齐基础能力 | `src/views/PopupView.vue` | 最近模块卡片会跳入快速发送模式：优先载入模块首个请求；若模块还没有请求，则从模块变量中选择 base/url/host/endpoint 或 URL 值，预填 `{{模块名.var}}` 作为 baseUrl，避免空模块点击后无反馈。 |
| 本轮 | Popup/SidePanel 打开上下文恢复 | 已补齐基础能力 | `src/utils/view-context.ts`, `src/views/PopupView.vue`, `src/views/SidePanelView.vue`, `src/views/MainView.vue` | Popup 从历史或最近模块打开全屏页/侧边栏时会携带当前 category/module/api 上下文；Main/SidePanel 启动后读取 URL/localStorage/chrome.storage 的短时上下文并恢复选中接口或模块，SidePanel 展开全屏也会保留当前上下文。 |
| 本轮 | KV 编辑器即时保存 | 已修复 | `src/components/common/KvEditor.vue` | 修复 value 输入时 `update` 未执行的问题，避免参数/Header/Cookie 值修改只停留在本地行状态。 |
| 本轮 | 响应面板 Cookie/实际请求/复制保存 | 已补齐 | `src/components/response/ResponsePanel.vue` | 新增 Cookie、实际请求 Tab，以及复制响应、保存响应按钮，对齐响应展示区关键操作。 |
| 本轮 | 响应状态栏与默认 Body 模式 | 已补齐基础能力 | `src/components/response/ResponsePanel.vue` | 状态栏除状态码/耗时/大小外补充响应头 Content-Type 摘要；新响应到达时按内容自动选择默认 Body 模式：JSON 走 Tree，可预览类型走 Preview，普通文本走 Pretty，避免非 JSON 响应因默认 tree 模式导致空白。 |
| 本轮 | Ctrl/Cmd+Enter 发送请求 | 已补齐 | `src/composables/useKeyboardShortcuts.ts`, `src/components/editor/RequestBar.vue` | 快捷键现在派发全局发送事件，由当前请求栏监听并执行发送。 |
| 本轮 | Body JSON 美化/压缩工具 | 已补齐基础能力 | `src/components/editor/BodyEditor.vue` | JSON Body 工具栏补齐“美化/压缩/动态值/行数/字符数”组合；美化/压缩会解析当前 JSON 并写回请求体，解析失败时在工具栏提示错误，保持只读模块禁用编辑。 |
| 本轮 | 零配置示例项目种子数据 | 已补齐基础能力 | `src/stores/app.ts` | 首次启动且 IndexedDB 完全为空时自动创建“示例项目”大类，包含 JSONPlaceholder 与天气 API 两个模块、3 个免鉴权示例请求、本地开发环境变量与基础后置断言；写入 `starterSeeded` 标记，避免用户清空后反复自动重建。 |
| 本轮 | 三视场跟随系统主题 | 已补齐基础能力 | `src/App.vue`, `src/composables/useSettings.ts`, `src/stores/app.ts` | 主题默认值改为 `system`，App 根组件启动时统一应用主题，使 Popup/SidePanel/全屏页都会读取本地主题设置并在 `prefers-color-scheme` 变化时跟随浏览器主题。 |
| 本轮 | 全屏页网络模式真实生效 | 已修复 | `src/views/MainView.vue` | 顶部“网络模式”下拉从独立 local settings 改为绑定 `store.settings.corsMode`，切换后立即保存到 IndexedDB；请求发送、数据源同步、批量发送等读取同一设置，避免 UI 修改不影响实际请求。 |
| 本轮 | 代理模式 URL 配置入口 | 已补齐基础能力 | `src/views/MainView.vue` | 全屏页网络模式选择“代理”时显示代理 URL 输入框，绑定 `store.settings.proxyUrl` 并在 change/blur 时保存；请求发送链路已复用该设置。 |
| 本轮 | 请求行 Method 彩色圆点与发送 hover | 已补齐基础能力 | `src/components/editor/RequestBar.vue` | 将原生 Method select 打磨为自定义下拉，当前值和每个选项都显示 GET/POST 等方法色圆点；发送按钮 hover 缩放 1.02，并在只读模块禁用 Method 修改，补齐请求行交互细节。 |
| 本轮 | Postman 请求方法脚本改写 | 已补齐基础能力 | `src/utils/pre-request.ts`, `src/components/editor/RequestBar.vue`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.request.method` 现在可在前置脚本中读取/赋值，且 Worker/iframe/直连三条执行路径都会把修改后的 Method 回传给真实请求发送与历史记录，补齐设计文档“获取/设置请求方法”。 |
| 本轮 | 脚本测试报告结构化展示与导出 | 已补齐基础能力 | `src/stores/app.ts`, `src/types/index.ts`, `src/components/editor/RequestBar.vue`, `src/components/response/ResponsePanel.vue` | `pm.test` 结果不再只混入 Console 日志；请求发送会收集结构化测试结果，响应面板新增“测试报告”Tab 展示总数/通过/失败/跳过和错误详情，Markdown 文档与单文件 HTML 响应报告也会带上测试报告，补齐脚本执行生命周期“生成测试报告”基础路径。 |
| 本轮 | `pm.sendRequest` timeout / followRedirects | 已补齐基础能力 | `src/utils/pre-request.ts`, `src/components/editor/RequestBar.vue`, `src/utils/http.ts`, `extension/background.js` | `pm.sendRequest({ timeout, followRedirects })` 现在会传入真实请求链路；普通页面 fetch 与扩展 background fetch 均支持 AbortController 超时中断，并把 `followRedirects:false` 映射为 `redirect:"manual"`，补齐设计文档 RequestConfig 的基础行为。 |
| 本轮 | `pm.request.url/body` 直接赋值兼容 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 前置脚本现在可直接执行 `pm.request.url = "..."`、`pm.request.body = {...}` 或 `pm.request.method = "POST"`；运行时通过 Proxy 保留既有 `pm.request.url.addQueryParams()` / `pm.request.body.urlencoded.add()` 对象 API，同时把直接赋值回写到真实请求，避免脚本覆盖内部 facade 后失效。 |
| 本轮 | 脚本编辑器重置操作 | 已补齐基础能力 | `src/components/editor/TabPanel.vue` | 前置/后置脚本工具栏新增“重置”按钮，清空前二次确认，并遵守只读模块禁用规则；与既有“运行脚本”和片段按钮共同补齐设计文档脚本编辑器工具栏基础操作。 |
| 本轮 | 脚本日志毫秒级格式 | 已补齐基础能力 | `src/components/response/ResponsePanel.vue`, `src/components/editor/TabPanel.vue` | 控制台与脚本 Tab 日志时间改为 `HH:mm:ss.SSS`，级别标签改为设计文档中的小写 `log/info/warn/error/table`，表格日志继续用结构化 table 渲染，对齐 6.6 日志输出格式。 |
| 本轮 | `ScriptTimeoutError` 超时错误类型 | 已补齐基础能力 | `src/utils/pre-request.ts` | Worker sandbox、iframe sandbox 与直连执行超时时统一创建 `ScriptTimeoutError`，错误消息包含 `ScriptTimeoutError:` 前缀，且超时判断从字符串散落判断收敛到 `isScriptTimeoutError()`，对齐 6.7 “超时熔断”错误类型要求。 |
| 本轮 | `pm.request.headers.add` 不覆盖同名 Header | 已修复 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 按设计文档约束，`pm.request.addHeader()` / `pm.request.headers.add()` 遇到已存在同名 Header（大小写不敏感）时保留原值；`set()` / `upsert()` 仍负责覆盖同名 Header，三条脚本执行路径保持一致。 |
| 本轮 | `pm.request.setPathParam` 模板占位兼容 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `setPathParam(key, value)` 现在除 `:key`、`{key}` 外，也会替换 URL 中的 `{{key}}` / `{{ key }}` 模板占位，方便设计文档中的 Path 参数请求变量与脚本改写同一占位格式打通。 |
| 本轮 | Ctrl/Cmd+N 新建请求 | 已补齐基础能力 | `src/types/index.ts`, `src/utils/shortcuts.ts`, `src/composables/useKeyboardShortcuts.ts`, `src/views/MainView.vue` | 自定义快捷键矩阵新增“新建请求”，默认 `Mod+N`；执行时会在当前模块或当前文件夹中新建空请求并选中，补齐设计文档请求管理快捷入口。 |
| 本轮 | 全屏页拖拽文件导入 | 已补齐基础能力 | `src/views/MainView.vue` | 全屏页监听窗口文件拖拽，显示导入覆盖层；支持自动识别 cURL、Postman Collection、OpenAPI/Swagger JSON/YAML 与 HAR 文件，导入后写入当前模块或按 folder/tag 创建模块并选中首个接口。 |
| 本轮 | 全局搜索覆盖模块/请求变量 | 已补齐基础能力 | `src/components/common/GlobalSearch.vue` | Ctrl/Cmd+K 搜索除接口、环境变量、历史外，新增跨模块变量和请求级变量索引；选中模块变量会跳到模块页，选中请求变量会跳到所属接口，补齐设计文档“搜索跨所有模块的接口、变量、历史”。 |
| 本轮 | 全屏页右侧面板 0–400px 拖拽调整 | 已补齐基础能力 | `src/views/MainView.vue` | 右侧信息面板新增拖拽分隔条，宽度限制在 0–400px；拖到 0 会收起，重新显示时恢复 300px，补齐设计文档三栏宽度规则。 |
| 本轮 | 左侧导航 200–400px 拖拽调整 | 已补齐基础能力 | `src/components/sidebar/Sidebar.vue` | 左侧接口目录保持 260px 默认与 48px 折叠，并新增右边缘拖拽分隔条，展开状态下宽度限制在 200–400px，补齐设计文档三栏宽度规则。 |
| 本轮 | 全局快捷键打开/聚焦 Popup 与全屏页 | 已补齐基础能力 | `extension/background.js`, `extension/manifest.json` | `Ctrl+Shift+A` 优先调用 `chrome.action.openPopup()`，失败时聚焦既有 `popup.html` 标签或新建；`Ctrl+Shift+F` / 右键入口打开全屏页时优先聚焦既有 `main.html`，避免重复开多个全屏页。 |
| 本轮 | Manifest 图标与 module service worker 对齐 | 已补齐基础能力 | `extension/manifest.json`, `extension/icons/generate.js`, `extension/icons/icon32.png` | Manifest action/icons 补齐设计清单中的 32px 图标，background service worker 增加 `type: module`；图标生成脚本改为 ESM 以适配仓库 `type: module`，并生成 `icon32.png`。 |
| 本轮 | Side Panel 并行调试台头部与 Network 捕获列表 | 部分补齐 | `src/views/SidePanelView.vue`, `extension/background.js`, `PendingImportBridge.vue` | 增加紧凑 topbar、折叠导航、全屏入口、最近 webRequest 捕获列表，并可一键放入待导入队列；捕获项可复制 cURL，也可拖拽为 cURL 文本并尝试写入剪贴板；新增 ResizeObserver 宽度断点，300–400px 自动图标栏、400–600px 隐藏接口树并提供接口下拉、600–800px 取消编辑区最小宽度以适配上下堆叠。 |
| 本轮 | Side Panel 接口树拖到网页 DOM | 部分补齐 | `src/components/sidebar/Sidebar.vue`, `extension/content.js`, `extension/manifest.json` | 接口树请求/文件夹拖拽时会携带 cURL 与 ApiFix payload，并通知当前标签页 content script 安装 30s 临时 drop 处理器；落到输入框/textarea/contenteditable 时插入 cURL，落到普通 DOM 时派发 `apifix:request-drop` 页面事件并显示提示。已补 Shadow DOM/受控输入插入、window+目标元素 composed 事件派发、页面侧自定义接收协议和子 frame 内容脚本注入基础；复杂低代码站点仍需专项适配。 |
| 本轮 | OpenAPI YAML / JSON 编辑模式 | 部分补齐 | `src/types/index.ts`, `src/components/editor/WorkspaceSettingsView.vue`, `src/utils/openapi-import.ts` | 模块可保存 OpenAPI 文本；JSON 与常见 YAML 可实时预览识别接口数量（轻量 YAML 解析不覆盖全部规范边界）。 |
| 本轮 | OpenAPI 模块双向同步 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/utils/export.ts`, `src/utils/openapi-import.ts` | OpenAPI YAML 模块页新增从当前可视化接口生成 YAML/JSON 到编辑器，以及把编辑器中的 OpenAPI/Swagger 文本按 Method + URL 增量导入/更新回当前模块；同步时会沿用/创建 tag 根文件夹并保存 `openapiText`。仍不宣称无损保留 OpenAPI 原文注释/排序/全部扩展字段，完整 YAML 1.2 仍待补。 |
| 本轮 | InterfaceNode 文件夹树与文件夹前置脚本 | 部分补齐 | `src/types/index.ts`, `src/db/index.ts`, `src/stores/workspace.ts`, `src/components/sidebar/Sidebar.vue`, `src/components/editor/RequestBar.vue` | InterfaceNode 支持 folder/request、parentId、无限层级展示；Sidebar 可新建文件夹/子文件夹/文件夹内请求，删除子树；发送请求前会按祖先文件夹顺序执行文件夹前置脚本。 |
| 本轮 | 文件夹批量发送与 Markdown 测试报告 | 已补齐基础能力 | `src/components/sidebar/Sidebar.vue`, `src/utils/http.ts`, `src/utils/pre-request.ts` | 文件夹右键菜单新增“批量发送（串行）/（并行）”，会按文件夹子树收集请求、套用环境/模块/请求级变量并执行祖先文件夹与接口前置脚本，写入请求历史后导出 Markdown 测试报告。 |
| 本轮 | 文件夹设置页 | 已补齐基础能力 | `src/components/sidebar/Sidebar.vue` | 文件夹右键新增“文件夹设置”弹窗，可修改文件夹名称、维护文件夹前置脚本，并展示子文件夹/子请求/脚本配置状态，替代仅能用短 prompt 编辑前置脚本的基础路径。 |
| 本轮 | 接口配置对比差异 | 已补齐基础能力 | `src/components/sidebar/Sidebar.vue` | 接口右键菜单新增“选为对比基准 / 与基准对比差异”，可并排展示 Method、URL、Params、Headers、Cookies、Auth、Body、请求变量和前后置脚本差异，补齐全屏页接口对比的基础路径。 |
| 本轮 | 全屏页文档视图 | 已补齐基础能力 | `src/views/MainView.vue` | 主工具栏新增“文档视图/编辑视图”切换；文档视图按当前模块只读聚合接口为 Swagger-like 卡片，展示 Method、URL、Query、Headers 和 Body，补齐全屏页文档浏览基础。 |
| 本轮 | 变量保存前校验拦截 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块保存与模块变量保存前会复用未解析变量扫描，列出缺失引用和受影响接口，并要求确认后才继续保存，补齐保存前强制校验基础路径。 |
| 本轮 | JSONPath bracket/通配符断言与提取 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue`, `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 单接口/场景断言和变量提取、脚本 `pm.response.jsonPath()` 均支持 `$.items[0].id`、`$['weird.key']`、`$.items[*].id` 等 bracket 与通配符路径。 |
| 本轮 | Side Panel 拖拽目标标签页选择 | 已补齐基础能力 | `src/views/SidePanelView.vue`, `src/components/sidebar/Sidebar.vue` | Side Panel 顶部新增当前窗口标签页选择器，接口树请求/文件夹拖拽时优先把临时 drop 处理器安装到选定标签页；无选择时回退当前激活页，补齐多标签精确目标基础。 |
| 本轮 | 页面侧请求拖拽富协议 | 已补齐基础能力 | `extension/content.js` | 内容脚本在准备拖拽时派发 `apifix:request-drop-ready`，落点时派发带 token/target/formats/acceptVia 的 `apifix:request-drop`；页面可同步 `postMessage({ source:'apifix-page', type:'APIFIX_ACCEPT_REQUEST_DROP', token })` 接管处理，未接管时回退插入 cURL/普通事件。 |
| 本轮 | 页面选中 JSON 富格式化 / 自动粘贴 Body | 已补齐基础能力 | `extension/background.js`, `extension/content.js`, `src/components/common/PendingImportBridge.vue` | 扩展右键菜单改为“发送选中 JSON 到 Side Panel 格式化”；选中内容会先解析为 JSON，写入 pending import、自动打开 Side Panel，并由 PendingImportBridge 创建 POST 请求且把格式化 JSON 放入 Body；页面内格式化浮层的“发送到 ApiFix”按钮也会打开侧边栏。 |
| 本轮 | 模块本地审计日志 | 已补齐基础能力 | `src/types/index.ts`, `src/db/index.ts`, `src/components/editor/WorkspaceSettingsView.vue` | 新增 `ModuleAuditLog` 与 Dexie v6 `moduleAuditLogs` 表；模块设置页展示最近 20 条审计日志，并记录模块保存、变量导入导出/保存、数据源同步、文档/OpenAPI/测试报告导出等关键操作。 |
| 本轮 | 模块 HTML 文档站导出 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块概览快捷侧栏和导出配置区新增“导出/发布 HTML 文档站”，生成可直接打开的单文件站点，包含模块文档、接口 Method/URL、Query、Headers、Cookies 与 Body。 |
| 本轮 | 用户自定义快捷键 | 已补齐基础能力 | `src/types/index.ts`, `src/utils/shortcuts.ts`, `src/composables/useKeyboardShortcuts.ts`, `src/views/MainView.vue`, `src/components/common/GlobalSearch.vue` | `AppSettings.customShortcuts` 写入 Dexie settings；全屏页新增快捷键设置弹窗，可录入/恢复/保存发送、保存、搜索、主题、右栏、历史、文档视图等组合键；快捷键执行改走统一 action 分发，搜索面板支持自定义事件打开。 |
| 本轮 | 脚本引擎 async / `pm.sendRequest` / Visualizer | 部分补齐 | `src/utils/pre-request.ts`, `src/components/editor/RequestBar.vue`, `src/stores/app.ts`, `src/components/response/ResponsePanel.vue` | 前置/后置脚本改为 AsyncFunction 执行，支持顶层 `await`、30s 异步超时、`pm.sendRequest`、`pm.sendRequest.sendInterface`、异步/同步 `pm.test`、更完整 `pm.expect` 子集、`pm.visualizer.template/table`，并在响应区新增 Visualize Tab。 |
| 本轮 | 扩展端 iframe sandbox 脚本主路径 | 部分补齐 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html` | Chrome 扩展环境下前置/后置脚本改走 sandbox iframe；sandbox 支持 async/await、环境变量变更追踪、`pm.test/expect` 子集、`pm.visualizer`、后置 `pm.response`，并通过 postMessage 桥接父页面的 `pm.sendRequest` / `sendInterface`。 |
| 本轮 | 脚本 sandbox 超时重置与普通页面启用 | 部分补齐 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html` | 前置/后置脚本在有 DOM 的普通网页/扩展环境统一优先走 iframe sandbox；iframe 加载有 ready 超时，脚本执行超时会移除并重建 sandbox，同时拒绝同 sandbox 未完成请求，降低后续执行被污染风险。浏览器同线程死循环的强制抢占仍不能完全保证，需后续 Worker/独立进程级隔离。 |
| 本轮 | Worker 级脚本硬中断 | 部分补齐 | `src/utils/pre-request.ts`, `extension/script-worker.js`, `build.js` | 新增专用 `script-worker.js` 沙箱，前置/后置脚本优先在 Worker 中执行；`pm.sendRequest`/`sendInterface` 通过主线程桥接，Visualizer/pm.test/pm.expect/环境变更保持；超时会 `worker.terminate()`，可硬中断死循环，Worker 不可用或非超时错误时回退 iframe sandbox。已补常用 `pm.request` 修改 API，并增加基于 `performance.memory` 的 128MB 增量堆内存守卫；仍缺跨浏览器精确内存计量和完整 Postman API。 |
| 本轮 | 脚本 Worker 内存守卫 | 部分补齐 | `src/utils/pre-request.ts` | Worker sandbox 执行期间按 250ms 采样 `performance.memory.usedJSHeapSize` 增量，默认超过 128MB 会终止 Worker 并返回错误；该能力依赖 Chromium 暴露的近似堆指标，因此记录为浏览器可用时的防护，不宣称跨浏览器精确内存计量。 |
| 本轮 | 脚本沙箱全局白名单/黑名单收敛 | 部分补齐 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html` | AsyncFunction 执行入口统一加 `'use strict'`，显式注入设计允许的 URL/URLSearchParams/TextEncoder/WeakMap/Proxy/Reflect/Symbol/Intl 等基础对象，并用参数遮蔽 `window/document/navigator/location/fetch/XMLHttpRequest/WebSocket/localStorage/indexedDB/eval/Function/globalThis/self/parent/top/opener/chrome` 等禁止对象。仍不能替代 Worker/进程级安全隔离。 |
| 本轮 | 脚本 Postman 变量作用域兼容 | 部分补齐 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 直连运行时、iframe sandbox 与 Worker sandbox 均新增 `pm.collectionVariables`、独立 `pm.globals`、本地 `pm.variables`、`pm.iterationData` 以及各作用域 `clear/toObject/replaceIn/import`；旧版 `postman.setGlobalVariable` 改为写入 globals，不再混写环境变量，并恢复 `CryptoJS.SHA256` 兼容。已补常用 `pm.request` 修改 API 和可用时 128MB 增量内存守卫，仍缺完整 Postman Runtime API 与跨浏览器精确内存计量。 |
| 本轮 | Postman 变量作用域 import 方法 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.environment/globals/collectionVariables/variables` 作用域补齐 `import(obj)` 批量写入方法；environment import 会记录变更 key，保持保存回调与沙箱结果同步。 |
| 本轮 | Postman 响应对象常用方法补齐 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.response` / `pm.sendRequest` 响应 facade 增加设计文档中的 `size`、`xml()`、`blob()`，与既有 `code/status/headers/responseTime/text/json/jsonPath/match/toJSON` 共同覆盖更多 Postman 风格响应脚本。 |
| 本轮 | Postman CookieJar 脚本 API | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 直连、iframe sandbox 与 Worker sandbox 均新增 `pm.cookies` / `pm.cookieJar` 基础能力：支持当前请求 Cookie 的 `get/has/set/unset/clear/toObject/all/each`，以及 `pm.cookies.jar().get/getAll/set/unset/clear/toObject(url, ...)` 的 Promise + callback 双风格；当前 URL 的 jar 写入会同步回请求 Cookie 结果。 |
| 本轮 | Postman Vault 脚本 API | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 直连、iframe sandbox 与 Worker sandbox 均新增 `pm.vault` / `postman.vault` 的本次执行内存兼容层，支持 `get/set/unset/clear/has/toObject`，并兼容 Promise 与 callback 双风格。当前实现不宣称安全持久化 Vault，仅用于避免常见 Postman 脚本因 `pm.vault` 缺失而失败。 |
| 本轮 | Postman Execution 脚本 API | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js`, `src/components/editor/RequestBar.vue` | 新增 `pm.execution` / `postman.execution`：`skipRequest()` 会在前置脚本中真正中止当前请求发送并保留脚本日志；`setNextRequest()` 记录下一请求意图；`runRequest()` 复用现有 `sendInterface` 桥接运行当前模块接口，兼容 Promise 与 callback 风格，并按官方限制单脚本最多调用 10 次；`location` / `location.current` 暴露当前大类/模块/接口路径。 |
| 本轮 | Postman 响应 headers / match 兼容 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.response.headers` / `pm.sendRequest` 响应 headers 现在既可 `get/has/all`，也可按原始 key 或小写 key 下标读取（如 `headers['content-type']`）；`match()` 从布尔判断改为返回首个匹配文本或 `null`，对齐设计文档正则提取语义。 |
| 本轮 | Chai 风格断言链补强 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.expect` / 全局 `expect` 在直连、iframe sandbox 与 Worker sandbox 中补齐 `.deep.equal/.that/.is/.and/.contain(s)/.key(s)/.within/.length/.null/.undefined/.throw` 等常用 Chai 链，提升设计文档断言示例兼容性。 |
| 本轮 | Postman 请求/集合别名补齐 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 设计文档示例中的 `pm.request.setHeader/getHeader/removeHeader`、`pm.request.addHeader/addQueryParam/removeQueryParam/setPathParam` 与 `pm.collection` 模块级变量别名已在直连、iframe sandbox、Worker sandbox 及 `postman` 兼容对象中补齐，继续保留 `pm.collectionVariables`。 |
| 本轮 | `pm.request.headers` 下标读写 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 设计文档把 `pm.request.headers` 定义为 `Record<string,string>`；现在请求 headers facade 通过 Proxy 支持 `pm.request.headers['X-Trace']='1'`、大小写不敏感读取、`delete pm.request.headers['X-Trace']`、`Object.keys/entries` 枚举，并继续保留 `add/set/upsert/get/remove/has/all/toObject/each/clear` 方法，三条脚本路径保持一致。 |
| 本轮 | `pm.request.body` 对象/FormData 写入桥接 | 已补齐基础能力 | `src/utils/pre-request.ts`, `src/components/editor/RequestBar.vue`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 设计文档将 `pm.request.body` 定义为 `string/object/FormData`；现在直接赋值对象、普通 Record KV、`entries()` 数据源、`{ mode:'json'|'raw'|'urlencoded'|'formdata' }`、`URLSearchParams` 或 `FormData` 会在脚本 facade 中归一化为 raw/urlencoded/formdata；raw/json 写入会清理旧表单字段，真实发送前自动把原 `none` Body 推断成 JSON/raw，或按字段切换到 form/urlencoded；同时补齐 `text/json/toObject/all/toString` 和顶层 `set/get/remove` 读取/维护接口；`pm.sendRequest({ body })` 也同步支持 `mode:'json'`/`content`、URLSearchParams/FormData、普通 Record KV 与字段清理，避免脚本写入只停留在日志结果而未实际发送。 |
| 本轮 | `pm.request.url` 字符串兼容接口 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 在保留 `addQueryParams/upsertQueryParams/removeQueryParams/allQueryParams` 对象 API 的同时，补齐 `raw/href` 读写、`toJSON/valueOf/toString/Symbol.toPrimitive` 和 `includes/startsWith/endsWith/match/replace` 字符串代理，降低设计文档 `url:string` 与 Postman URL facade 之间的脚本兼容落差。 |
| 本轮 | `pm.test.skip` 跳过用例 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 设计文档 API 中的 `pm.test.skip(name, fn?)` 已在三条脚本执行路径补齐，跳过项会写入测试结果并标记 `skipped`，不执行回调。 |
| 本轮 | Visualizer 表格签名对齐 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.visualizer.table(data, options?)` 在 iframe/Worker sandbox 中改为兼容设计文档的数据优先签名，同时保留旧的 `table(title, rows)`；直连运行时补齐 `visualizer.set()` 别名。 |
| 本轮 | Visualizer EJS 插值 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.visualizer.template()` 的轻量模板渲染除既有 `{{path}}` 外，新增设计示例使用的 `<%= path %>` 数据路径插值（如 `<%= data %>` / `<%= data.token %>`），直连、iframe sandbox、Worker sandbox 保持一致。 |
| 本轮 | 变量 `set` 指定作用域 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.environment/globals/collectionVariables/collection/variables.set(key, value, scope)` 支持 `global/module|collection/local|variables/environment|env` 路由，补齐设计文档 VariableScope 指定作用域写入能力。 |
| 本轮 | `pm.info` 脚本上下文 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js`, `src/components/editor/RequestBar.vue` | 直连、iframe sandbox 与 Worker sandbox 均暴露 `pm.info` / `postman.info`；请求栏执行脚本时注入当前大类、模块、接口/文件夹名与 `prerequest/test` 阶段。 |
| 本轮 | 响应 Preview / JSON 表格 / Diff | 部分补齐 | `src/components/response/ResponsePanel.vue` | Body 区新增 Preview、Table 模式；支持 HTML/SVG iframe 预览、图片/PDF data-url 预览、JSON 数组/对象表格化；新增 Diff Tab，对比上一次与当前响应的 JSON 路径或文本行差异。 |
| 本轮 | 数据源 OpenAPI JSON 手动同步 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/utils/http.ts`, `src/utils/openapi-import.ts` | “立即同步”现在会通过现有请求通道拉取数据源 URL，保存原始 OpenAPI 文本，按 Method + URL 增量更新已有接口或新增接口，并按 OpenAPI tag 创建根文件夹；同步日志显示新增/更新/失败结果。 |
| 本轮 | 数据源字段映射应用 | 已补齐基础能力 | `src/types/index.ts`, `src/utils/openapi-import.ts`, `src/utils/data-source-sync.ts`, `src/utils/export.ts`, `src/components/editor/WorkspaceSettingsView.vue` | 数据源同步会按模块配置的 `fieldMapping` 将 OpenAPI `operationId/summary/description/tags[0]` 等字段映射到接口名称、接口描述与文件夹分类；默认 `operationId=接口名称`、`summary=接口描述`、`tags[0]=文件夹分类` 已落地，更新已有接口时也会迁移到映射后的根文件夹。接口描述已进入 OpenAPI/Markdown/HTML 文档导出与模块 HTML 文档站展示。 |
| 本轮 | Sidebar 接口 hover 快捷发送 / 复制 cURL | 已补齐 | `src/components/sidebar/Sidebar.vue`, `src/utils/export.ts`, `src/composables/useKeyboardShortcuts.ts` | 接口行 hover 时显示快速发送与复制 cURL 按钮；快速发送复用全局发送事件，复制 cURL 复用现有导出工具。 |
| 本轮 | 请求发送下拉动作 | 已补齐 | `src/components/editor/RequestBar.vue`, `src/components/common/CodeGenPanel.vue` | 更多操作新增“发送并下载响应”和“发送后生成代码”；前者发送成功后按响应 Content-Type 下载，后者发送成功后自动打开代码生成面板。 |
| 本轮 | OpenAPI YAML 轻量解析 | 部分补齐 | `src/utils/openapi-import.ts`, `src/components/editor/WorkspaceSettingsView.vue` | 在不新增依赖前提下增加基础 YAML 解析器，支持常见 OpenAPI/Swagger YAML 的实时预览、导入和数据源同步；本轮继续补 flow object/array、anchors/aliases/merge key、path-level parameters、Swagger 2.0 body/formData 参数、OpenAPI requestBody examples/form fields、schema allOf/oneOf/anyOf/default/enum 示例解析，并修复数组项内 `key: |` / `key: >` 块标量解析。仍不宣称覆盖全部 YAML 1.2 边界。 |
| 本轮 | YAML 数组项块标量解析 | 已补齐基础能力 | `src/utils/openapi-import.ts` | YAML 轻量解析器新增共享块标量读取逻辑，数组项中的 `- description: |`、`- example: >` 等 OpenAPI 常见写法会解析为字符串，而不是误走子对象解析，提升描述/示例文本导入成功率。 |
| 本轮 | 数据源外部 `$ref` 合并 | 部分补齐 | `src/utils/openapi-import.ts`, `src/utils/data-source-sync.ts` | OpenAPI 解析器拆出 `parseOpenApiSpec` / `importOpenApiSpec`；数据源同步会扫描非本地 `$ref`，最多递归拉取 24 个相对/绝对外部引用，合并常见 `components.schemas/parameters/requestBodies`、Swagger `definitions/parameters`，并把外部根 JSON Schema 或带 JSON Pointer 的 schema 文档落入本地 `components.schemas` 后改写引用。已补本地 schema `$ref` 循环与过深递归保护；仍缺完整 YAML 1.2 与所有 JSON Schema 方言关键字无损语义。 |
| 本轮 | JSON Schema 示例生成常见方言关键字 | 已补齐基础能力 | `src/utils/openapi-import.ts` | OpenAPI 导入请求体/表单/参数（含 OpenAPI 3 `content` 参数）示例生成新增 `type` 数组、`nullable`、`const`、`examples`、`additionalProperties`、`minItems/maxItems`、`prefixItems`、`minProperties/maxProperties`、`minLength/maxLength`、`format`、简单 `pattern`、`readOnly` 请求字段过滤、`minimum/exclusiveMinimum/maximum/exclusiveMaximum/multipleOf` 等常见 JSON Schema / OpenAPI 关键字处理，减少复杂 schema 导入后示例为空或过度退回 `null`。 |
| 本轮 | OpenAPI 本地 `$ref` 循环保护 | 已补齐基础能力 | `src/utils/openapi-import.ts` | `resolveSchema` 增加本地 `$ref` visited 集合与最大递归深度保护，并补 JSON Pointer `~1/~0` 解码，避免递归 schema 示例生成时被循环引用卡死。 |
| 本轮 | Sidebar 拖拽移动接口/文件夹/模块 | 已补齐 | `src/stores/workspace.ts`, `src/components/sidebar/Sidebar.vue` | 增加持久化移动接口：接口/文件夹可拖入文件夹或模块根部，拖到请求行可按目标位置排序；模块可拖到大类头部完成跨大类移动；拖拽时会高亮分类/模块根部/文件夹/插入线落点。 |
| 本轮 | Sidebar 48px 折叠模式 | 已补齐 | `src/components/sidebar/Sidebar.vue`, `src/assets/styles/theme.css` | Sidebar 自身新增折叠按钮与 48px 图标模式，隐藏搜索/动作/文字，仅保留图标、展开箭头和 Method 标识，对齐设计规范中的可折叠侧栏。 |
| 本轮 | 全局搜索键盘发送 | 已补齐 | `src/components/common/GlobalSearch.vue`, `src/composables/useKeyboardShortcuts.ts`, `src/components/editor/RequestBar.vue` | Ctrl/Cmd+K 搜索面板支持上下键选择，Enter 会选中并发送当前/第一条接口或历史结果，Esc 关闭。 |
| 本轮 | Popup / Side Panel Esc 收起 | 已补齐 | `src/views/PopupView.vue`, `src/views/SidePanelView.vue` | Popup 监听 Esc 并关闭自身；Side Panel 监听 Esc，优先关闭 Network 捕获浮层，其次折叠左侧导航。 |
| 本轮 | 脚本 Tab 运行/片段/日志面板 | 已补齐 | `src/components/editor/TabPanel.vue`, `src/stores/app.ts`, `src/utils/pre-request.ts` | 前置/后置脚本 Tab 内新增运行按钮、常用 Postman 风格代码片段插入，以及最近脚本日志预览。 |
| 本轮 | Sidebar 删除确认 / 双击设置 | 已补齐 | `src/components/sidebar/Sidebar.vue` | 删除请求、文件夹、模块、分组前均二次确认；分组/模块双击可明确进入对应设置视图。 |
| 本轮 | 扩展端 chrome.cookies 自动携带 | 部分补齐 | `extension/background.js`, `extension/manifest.json`, `src/utils/http.ts` | 修复 background 语法问题；扩展 API/stream/download 请求在开启自动携带 Cookie 时会通过 `chrome.cookies.getAll({ url })` 注入目标站点 Cookie。 |
| 本轮 | Swagger UI / HTML 规格地址自动发现 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/utils/http.ts`, `src/utils/openapi-import.ts` | 数据源同步若拉到 HTML/Swagger UI 页面，会从 `url/configUrl/openapi/swagger/api-docs` 等常见片段发现 JSON/YAML 规格候选地址并二次拉取导入。 |
| 本轮 | 数据源自动同步调度 / Webhook 触发 | 部分补齐 | `src/utils/data-source-sync.ts`, `src/components/common/DataSourceAutoSyncBridge.vue`, `src/components/editor/WorkspaceSettingsView.vue`, `src/App.vue`, `src/types/index.ts`, `extension/background.js` | 抽出数据源同步服务；模块数据源新增自动同步间隔、Webhook 密钥、上次/下次同步时间、状态、消息与实际来源 URL；应用启动后每分钟扫描 `auto` 模块，使用本地锁避免多视图重复同步；扩展端支持 `TRIGGER_DATA_SOURCE_SYNC` / `APIFIX_TRIGGER_DATASOURCE_SYNC` runtime/external 消息触发 webhook 策略模块同步，并在配置了密钥时拒绝无密钥/错密钥触发。 |
| 本轮 | 数据源 Webhook 触发密钥 | 已补齐基础能力 | `src/types/index.ts`, `src/components/editor/WorkspaceSettingsView.vue`, `src/components/common/DataSourceAutoSyncBridge.vue`, `extension/background.js` | `ModuleDataSource` 新增 `webhookSecret`；数据源策略为 Webhook 时设置页显示密钥输入；扩展 runtime/external 触发会转发 `secret`，桥接组件在模块配置了密钥时校验后才同步。 |
| 本轮 | 二进制响应保真预览/保存 | 部分补齐 | `src/types/index.ts`, `src/utils/http.ts`, `extension/background.js`, `src/utils/binary-response.ts`, `src/components/response/ResponsePanel.vue`, `src/components/editor/RequestBar.vue` | 普通请求与扩展请求对图片/PDF/音视频/压缩包等二进制响应改用 ArrayBuffer→base64 保存，响应面板 Preview/保存/发送并下载按真实二进制 Blob 处理，不再把二进制当 UTF-8 文本破坏。 |
| 本轮 | 响应生成 Markdown 文档 | 已补齐 | `src/components/response/ResponsePanel.vue`, `src/utils/export.ts` | 响应区新增“生成文档”按钮，会组合当前请求 Markdown、响应状态/耗时/大小、Headers、Body 示例与实际请求 JSON，导出 `api-response-doc-*.md`。 |
| 本轮 | 模块/接口设计模型补字段 | 部分补齐 | `src/types/index.ts`, `src/stores/workspace.ts`, `src/components/editor/WorkspaceSettingsView.vue`, `src/components/editor/RequestBar.vue`, `src/components/sidebar/Sidebar.vue` | `Module` 增加 stats/moduleType/exportConfig/meta；workspace 新建/迁移/更新时补默认模型元数据；设置页可保存默认导出格式/自动备份目标；`InterfaceNode` 增加 preScript/postScript/exportConfig 别名并与 ApiConfig 脚本同步。 |
| 本轮 | 文档/模型/测试用例 IndexedDB 表 | 部分补齐 | `src/types/index.ts`, `src/db/index.ts` | 新增 `ModuleDocArtifact`、`ModuleDataModel`、`InterfaceTestCase` 类型和 Dexie v4 表 `moduleDocs/moduleModels/interfaceTestCases`，为设计中的文档、模型、测试覆盖率统计提供独立持久化承载。 |
| 本轮 | History 模块/接口索引对齐 | 已补齐基础能力 | `src/types/index.ts`, `src/db/index.ts`, `src/stores/app.ts`, `src/components/common/HistoryPanel.vue`, `src/utils/migration.ts` | `HistoryEntry` 新增 `moduleId/interfaceId` 元数据；Dexie v7 为 history 增加 `moduleId/interfaceId` 与 `[moduleId+timestamp]` / `[interfaceId+timestamp]` 索引，并在升级时按已有 InterfaceNode 回填；新历史写入时自动反查模块/接口，历史面板优先用该元数据恢复路径和选中接口，对齐设计稿 history 按模块/接口索引的存储结构。 |
| 本轮 | 文档/模型/测试用例管理 UI 与单接口运行器 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/db/index.ts`, `src/types/index.ts`, `src/utils/http.ts` | 模块页新增“文档/模型/用例”Tab，可对模块/接口文档、JSON Schema 数据模型、单接口测试用例做新增/编辑/删除；概览统计改读真实 Dexie 表数据；用例可按当前环境变量发起真实请求并校验状态码、Body/Header/JSON 路径基础断言，回写最近运行结果。复杂 JSONPath bracket/通配符已补基础能力，常用断言 DSL 已补，后续仅剩更完整 Postman 兼容断言生态。 |
| 本轮 | 场景用例编排与运行报告 | 部分补齐 | `src/types/index.ts`, `src/db/index.ts`, `src/components/editor/WorkspaceSettingsView.vue` | 新增 `ModuleScenarioCase` / `ModuleScenarioStep` 类型与 Dexie v5 表 `moduleScenarioCases`；模块“文档/模型/用例”页可将单接口用例编排成场景，支持新增/编辑/删除、单场景/全部场景运行、失败短路或失败继续、通过数/失败原因报告回写，并把模块概览的场景用例总数与场景覆盖率改为真实数据。复杂 JSONPath bracket/通配符和逐步骤启用/失败继续策略 UI 已补基础能力，可视化提取器和常用断言 DSL 已补基础能力，后续仅剩更完整 Postman 兼容断言生态。 |
| 本轮 | 场景步骤变量提取/传递 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/stores/app.ts` | 单接口用例断言新增 `env.token=$.data.token` / `env.foo<-header.content-type` 风格提取语法；单用例运行会写入当前环境，场景运行会写入场景局部变量并供后续步骤通过 `{{token}}` 使用。复杂 JSONPath bracket/通配符和可视化提取器已补基础能力。 |
| 本轮 | 场景局部变量与失败继续策略 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/types/index.ts` | 场景运行时新增局部变量作用域：步骤提取出的 `env.xxx` 先写入场景内存，后续步骤通过 `{{xxx}}` 使用，不再污染全局当前环境；场景编辑器新增“失败后继续运行”开关并写入每个步骤的 `continueOnFailure`。复杂 JSONPath bracket/通配符、逐步骤启用/失败继续策略 UI 和可视化提取器已补基础能力。 |
| 本轮 | 场景逐步骤策略 UI | 已补齐基础能力 | `src/types/index.ts`, `src/components/editor/WorkspaceSettingsView.vue` | `ModuleScenarioStep` 新增 `enabled`；场景编辑器把步骤从单纯用例 ID 改为步骤草稿，可逐步设置启用/跳过和失败继续；运行时只执行启用步骤，报告按启用步骤统计，并保留跳过步骤摘要。 |
| 本轮 | 测试断言 DSL 扩展 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 单接口/场景用例断言从 status/body/header/json 基础等值扩展为 `status in`、数值比较、`exists/not exists`、`includes/not includes`、`matches`、JSONPath `length` 等常用 DSL；说明与 placeholder 同步更新。 |
| 本轮 | 测试变量可视化提取器 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 单接口测试用例编辑区新增“可视化变量提取器”，可选择 JSONPath/Header/Body 来源并填写变量名，一键生成 `env.xxx<- $.path` / `env.xxx<- header.name` 提取断言，场景运行时继续写入局部变量供后续步骤引用。 |
| 本轮 | Postman 请求 API 常用方法扩展 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 直连、iframe sandbox 与 Worker sandbox 的 `pm.request.headers` 新增 `upsert/toObject/each/clear`，`pm.request.url` 新增 `upsertQueryParams/removeQueryParams/allQueryParams/toString`，`pm.request.body.urlencoded/formdata` 新增 `upsert/remove/all`，并保持 raw `update`。 |
| 本轮 | Postman 请求 Cookie 脚本 API | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js`, `src/components/editor/RequestBar.vue`, `src/components/sidebar/Sidebar.vue` | 前置脚本现在会接收当前请求 Cookies，并支持 `pm.request.cookies.add/upsert/set/get/remove/has/all/toObject/each/clear` 及顶层 `pm.request.addCookie/setCookie/getCookie/removeCookie`；脚本修改后的 Cookies 会传回单请求发送和文件夹/模块批量发送链路。 |
| 本轮 | Postman 响应 JSON 断言补强 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.response.to.have` 新增 `jsonBody(path?, expected?)` 和 `bodyContains(text)`，直连、iframe sandbox 与 Worker sandbox 均可校验响应体为 JSON、按 JSONPath 取值并与期望值比较，补齐常见测试脚本断言入口。 |
| 本轮 | Chai 断言链常用别名补强 | 已补齐基础能力 | `src/utils/pre-request.ts`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | `pm.expect` 兼容链新增 `empty`、`finite`、`least/gte`、`most/lte`、`members`、`satisfy`、`ownProperty/ownPropertyDescriptor`，并让对象 `lengthOf` 支持 key 数、`deep.property(name, expected)` 支持深比较，提升 Postman 测试脚本迁移成功率。 |
| 本轮 | 模块测试报告导出 | 已补齐 | `src/components/editor/WorkspaceSettingsView.vue` | 场景用例区新增“导出测试报告”，生成 Markdown 报告，包含导出时间、接口/单接口/场景覆盖率、每个单接口用例断言与最近运行结果、每个场景的步骤链路、通过数和失败原因。 |
| 本轮 | Gist/WebDAV 模块备份 | 部分补齐 | `src/types/index.ts`, `src/stores/workspace.ts`, `src/components/editor/WorkspaceSettingsView.vue` | `exportConfig` 增加云端备份地址、Token、文件名；模块设置页可选择 Local/GitHub Gist/WebDAV，执行备份会导出完整模块 payload，本地下载、Gist POST/PATCH 或 WebDAV PUT；开启自动备份后保存模块设置会触发备份。 |
| 本轮 | 团队备份角色与冲突检测 | 部分补齐 | `src/types/index.ts`, `src/stores/workspace.ts`, `src/components/editor/WorkspaceSettingsView.vue` | `ModuleExportConfig` 增加 `teamRole`、`conflictStrategy` 与细粒度 `permissions`；模块备份 payload 写入本地更新时间/冲突策略；Gist PATCH 与 WebDAV PUT 前会拉取远端备份，若远端更新时间较新且策略为 prompt 则打开字段级冲突预览，用户可取消保留远端或确认覆盖；Viewer 与权限矩阵会拦截模块设置、变量保存、数据源同步和备份写入。已补本地审计日志、本地细粒度权限、远端较新冲突预览/覆盖决策基础，并新增字段级选择合并后写回 Gist/WebDAV 备份；已补本地三视图协同光标 presence，完整 CRDT 式多人合并仍非本地轻量范围。 |
| 本轮 | 模块细粒度权限矩阵 | 已补齐基础能力 | `src/types/index.ts`, `src/stores/workspace.ts`, `src/components/editor/WorkspaceSettingsView.vue` | `ModuleExportConfig.permissions` 持久化保存模块设置/变量/数据源同步/备份四类权限；设置页提供权限矩阵，运行时通过 `assertModulePermission` 拦截无权操作，Viewer 默认全只读。 |
| 本轮 | 备份冲突预览决策 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | Gist/WebDAV 备份遇到远端较新时，不再只静默中止；会展示远端/本地更新时间、接口/文档/模型/用例/场景数量，用户可选择继续覆盖或取消，备份 payload 也纳入文档/模型/用例/审计日志。 |
| 本轮 | 备份字段级冲突预览 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 远端备份较新时改为弹出冲突预览 Modal，按模块名称/说明/类型、接口/文档/模型/用例数量、模块变量、导出权限配置、数据源配置逐字段展示远端与本地差异，再选择取消备份或覆盖远端。 |
| 本轮 | 备份字段级合并写回 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 远端备份较新时，冲突 Modal 现在可逐字段勾选“使用远端”，并在写回 Gist/WebDAV 前生成合并后的备份 payload；支持模块名称/说明/类型、接口+API、文档、模型、单接口/场景用例、模块变量、权限配置、数据源配置的字段级选择。 |
| 本轮 | 响应 HTML 报告模板 | 已补齐基础能力 | `src/components/response/ResponsePanel.vue` | 响应区新增“HTML 报告”导出，生成带状态色、指标卡、Headers、Body 与 Actual Request 的单文件 HTML 报告，补齐比 Markdown 更适合分享/归档的专业报告模板基础。 |
| 本轮 | OpenAPI 权限化分享元数据 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块“分享 OpenAPI/导出 OpenAPI”生成的 JSON 增加 `x-apifix-share` 扩展，包含 moduleId/moduleName/exportedAt/teamRole/permissions/conflictStrategy，便于接收方识别本地分享权限与冲突策略。 |
| 本轮 | 模块主页分享按钮 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块主页头部在保存按钮旁新增“分享”按钮，直接导出带 `x-apifix-share` 元数据的 OpenAPI JSON，对齐设计稿 `[保存] [分享]` 的模块级快捷分享入口。 |
| 本轮 | OpenAPI Security 导入 Auth 映射 | 已补齐基础能力 | `src/utils/openapi-import.ts` | OpenAPI 3 `components.securitySchemes`、Swagger 2 `securityDefinitions` 与 operation/global `security` 会导入到接口 Auth：支持 HTTP Bearer、HTTP Basic 与 apiKey header/query，并用 `{{schemeName}}` 变量占位保留密钥。 |
| 本轮 | OpenAPI OAuth/OpenID Security 降级映射 | 已补齐基础能力 | `src/utils/openapi-import.ts` | 当前设计 AuthConfig 仅支持 none/bearer/basic/apikey，因此 OpenAPI `oauth2` / `openIdConnect` security scheme 导入时会保守映射为 Bearer Token，占位变量为 `{{schemeName.accessToken}}`，避免导入后完全丢失认证意图；完整授权码/PKCE/刷新流程仍属于后续 OAuth helper 范围。 |
| 本轮 | OpenAPI Path 参数请求变量导入 | 已补齐基础能力 | `src/utils/openapi-import.ts` | OpenAPI/Swagger 路径 `/users/{id}` 导入时会转为 `/users/{{id}}`，并把 path 参数默认值/example/enum 写入请求级变量，保持 URL 模板与请求变量优先级一致。 |
| 本轮 | OpenAPI Cookie 参数导入导出 | 已补齐基础能力 | `src/utils/openapi-import.ts`, `src/utils/export.ts` | OpenAPI/Swagger `in: cookie` 参数导入时会写入 Cookies 编辑器，并在导出 OpenAPI 时把启用 Cookie 写回 `parameters[].in = cookie`，保留说明与示例值，避免 Cookie 参数在 OpenAPI 往返迁移中丢失。 |
| 本轮 | OpenAPI 本地权限分享链接 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块主页和快捷侧栏新增“复制链接/复制权限链接”，会把带 `x-apifix-share` 权限元数据的 OpenAPI spec 编码到 `#apifix-share=` 本地片段链接；剪贴板不可用时下载 `.share-link.txt`，并写入模块审计日志。 |
| 本轮 | OpenAPI 权限分享链接接收 | 已补齐基础能力 | `src/components/common/PendingImportBridge.vue` | 应用启动或 hash 变化时会识别 `#apifix-share=`，解码带 `x-apifix-share` 的 OpenAPI payload，创建/复用分享模块并导入接口，导入后自动选中首个接口并清理 hash，形成本地分享链接生成→消费闭环。 |
| 本轮 | 分享权限元数据落地 | 已补齐基础能力 | `src/components/common/PendingImportBridge.vue` | 接收 `#apifix-share=` 时会把 `x-apifix-share.teamRole/permissions/conflictStrategy` 写入导入模块的 `exportConfig`；viewer 分享会落为 readonly 模块，避免权限元数据只存在于 OpenAPI payload 中。 |
| 本轮 | 本地分享链接过期校验 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue`, `src/components/common/PendingImportBridge.vue` | `x-apifix-share` 元数据新增 `expiresAt`/`ttlDays=7`，接收端在导入前检查过期时间，过期链接会提示重新生成并清理 hash；该能力覆盖本地片段链接有效期，服务端权限校验仍需后端支持。 |
| 本轮 | 三视图同步冲突提示 | 已补齐基础能力 | `src/components/common/CrossViewSyncBridge.vue` | BroadcastChannel/chrome runtime 同步桥新增跨视图同步 Toast；若本视图 3 秒内也修改同一 scope，会提示“另一视图同时修改”，并按 IndexedDB 最新状态刷新，补齐本地多视图冲突可见性基础。 |
| 本轮 | 跨视图编辑活动提示 | 已补齐基础能力 | `src/components/common/CrossViewSyncBridge.vue`, `src/components/editor/TabPanel.vue` | 请求编辑区切换 Tab、聚焦或点击时会广播当前接口与编辑 Tab；其他 Popup/SidePanel/全屏页通过 BroadcastChannel/chrome runtime 显示“另一视图正在编辑”提示，补齐本地多视图活动感知基础；本轮继续补字段/选区行列号 presence。 |
| 本轮 | 跨视图协同光标 Presence | 已补齐基础能力 | `src/components/common/CrossViewSyncBridge.vue`, `src/components/editor/TabPanel.vue`, `extension/background.js` | TabPanel 会采集当前接口、Tab、字段、选区起止与行列号，并通过 BroadcastChannel / runtime 中继到其他视图；CrossViewSyncBridge 展示“协同光标”浮层并自动过期远端 presence。该能力覆盖本地三视图字符位置感知，完整 CRDT/多人同文档合并仍非本地轻量范围。 |
| 本轮 | 页面拖拽复杂输入适配 | 已补齐基础能力 | `extension/content.js` | 接口拖到网页时会递归解析 Shadow DOM 命中元素和深层 activeElement；对 input/textarea 使用原生 value setter 触发 React/Vue 受控输入更新，并同时向 window 与目标元素派发 composed `apifix:request-drop` 事件，提升复杂站点接收兼容性。 |
| 本轮 | iframe 页面拖拽覆盖 | 已补齐基础能力 | `extension/manifest.json` | 扩展内容脚本新增 `all_frames` 与 `match_about_blank`，使 `APIFIX_PREPARE_INTERFACE_DROP`、drop-ready、Shadow DOM/受控输入插入逻辑可进入普通子 iframe 和继承父页 origin 的 about:blank/srcdoc frame，补齐跨 frame 拖拽投递基础。 |
| 本轮 | 低代码编辑器拖拽插入 fallback | 已补齐基础能力 | `extension/content.js` | 拖拽投递会在目标容器内查找 CodeMirror `.cm-content`、ProseMirror、Quill、Monaco `textarea.inputarea`、`role=textbox`、contenteditable、input/textarea 等嵌套编辑器，并优先派发合成 paste 事件；未被接收时再回退原生插入，提升低代码/富文本/代码编辑器接收成功率。 |
| 本轮 | 页面上下文元数据携带 | 已补齐基础能力 | `extension/content.js`, `src/components/common/PendingImportBridge.vue` | 内容脚本采集页面/选中内容时新增 referrer、浏览器语言、视口尺寸、frame URL/name/isTop 等上下文字段；待导入桥会把这些元信息写入请求级变量 `page.*`，让 Popup/SidePanel/全屏页从页面上下文创建请求后仍能引用来源环境。 |
| 本轮 | 请求管理批量 Header/Query 编辑 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块概览新增“请求批量编辑”面板，可对当前模块所有接口按 Key 批量新增/覆盖、仅缺失新增、仅替换已有或删除 Header / Query 参数；Header 匹配不区分大小写，应用前展示影响接口数并二次确认，执行后写入模块审计日志。 |
| 本轮 | 模块行 hover 快捷发送/复制 | 已补齐基础能力 | `src/components/sidebar/Sidebar.vue` | 左侧模块行 hover 新增“发送全部/复制模块 cURL/设置”快捷操作；模块右键菜单新增串行/并行发送全部、复制模块 cURL、移动到大类，请求会复用文件夹批量发送链路并导出 Markdown 测试报告。 |
| 本轮 | 代码生成与文档 Cookies 保真 | 已补齐基础能力 | `src/utils/export.ts` | cURL 导出新增 `-b` Cookie 串，Python requests 生成 `cookies={...}` 并传入请求，fetch/axios/Java HTTP Client 在无显式 Cookie Header 时补 Cookie Header；Markdown/HTML 文档也增加 Cookies 表，避免 Cookies Tab 在代码生成和文档导出时丢失。 |
| 本轮 | 三视场状态广播同步 | 部分补齐 | `src/components/common/CrossViewSyncBridge.vue`, `src/App.vue`, `extension/background.js` | 新增 BroadcastChannel + chrome.runtime 中继同步桥，环境变量、模块/接口树、设置、API 配置变更会广播到 Popup/SidePanel/全屏页并从 IndexedDB 重载，减少多视图编辑状态滞后；当前接口/Tab 编辑活动也会广播提示。 |
| 本轮 | 全屏页右侧 300px 信息面板 | 已补齐 | `src/views/MainView.vue` | 全屏页新增可隐藏右侧面板（Ctrl/Cmd+Shift+R），展示模块类型/数据源、当前环境变量、动态值快捷复制和接口 ID/创建/修改时间。 |
| 本轮 | 模块概览趋势与快捷侧栏 | 部分补齐 | `src/components/editor/WorkspaceSettingsView.vue`, `src/components/editor/RequestBar.vue` | 模块概览新增近 7 日调试趋势图（基于当前模块接口历史请求次数）和快捷侧栏入口，可直接导出 Markdown 文档素材、单文件 HTML 文档站、分享 OpenAPI JSON、跳转测试报告维护，并可打开首个接口的代码生成面板；趋势图已补 hover tooltip、相对峰值百分比和峰值高亮。OpenAPI 分享已补 `x-apifix-share` 权限元数据、本地片段权限分享链接、权限元数据落地、7 天本地过期校验及接收导入闭环，仍缺服务端权限校验。 |
| 本轮 | 模块趋势 hover 细节 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 近 7 日调试趋势柱状图新增 hover 详情卡，展示日期、次数、相对峰值百分比和本周峰值标记，提升模块概览数据可读性。 |
| 本轮 | 请求级变量 UI 与作用域优先级 | 已补齐主要功能 | `src/types/index.ts`, `src/stores/app.ts`, `src/components/editor/TabPanel.vue` | `ApiConfig` 增加 `requestVariables`；请求编辑区新增“请求变量”Tab，可维护当前接口临时变量；`store.getEnvVariables()` 在模块本地/远程值和全局环境变量之后叠加启用的请求变量，使 `{{变量名}}` 按设计获得请求级最高优先级，并继续支持 `{{模块名.varName}}` 跨模块引用。跨模块补全、静态校验和模块变量导入/导出已补齐基础能力。 |
| 本轮 | 变量自动补全跨作用域提示 | 已补齐基础能力 | `src/composables/useVariableAutocomplete.ts`, `src/components/common/VariableAutocomplete.vue` | `{{` 自动补全来源从仅环境变量扩展为请求变量、当前模块变量、全局环境变量、`模块名.变量名` 跨模块引用和动态函数，并按请求级优先级去重展示；引用失效静态提示与模块变量 JSON 导入/导出已接入模块变量页。 |
| 本轮 | 模块变量批量导入导出 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块变量页新增 JSON 导出与文件导入，支持 `{ variables: {...} }` 完整 payload 或直接变量对象，保留 remote/local/description/environmentValues，便于团队迁移与备份。 |
| 本轮 | 模块变量远端备份隐私与自动同步 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | Gist/WebDAV 远端备份前会清空模块变量 `local` 值并写入 `remotePrivacy.localModuleVariableValuesUploaded=false`，避免本地私有值自动上传；模块变量保存/导入后若开启自动备份且目标为 Gist/WebDAV，会自动触发远端备份，使远程变量变更按设计同步到云端。 |
| 本轮 | 变量引用静态校验 | 已补齐主要功能 | `src/components/editor/WorkspaceSettingsView.vue` | 模块变量页新增未解析变量引用检查，会扫描当前模块接口 URL/Header/Cookie/Params/Body/Auth/请求变量中的 `{{...}}`，按请求变量、模块变量、环境变量、跨模块变量和动态函数判断是否可解析，并列出缺失变量及受影响接口；模块设置/变量保存前会弹出校验确认；未解析面板可一键把缺失引用批量补到模块变量草稿，补齐跨接口变量修复基础路径。 |
| 本轮 | 变量缺失批量补齐 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 未解析变量引用面板新增“批量补到变量草稿”，会按规范化变量名去重，把缺失变量追加到模块变量表并标注影响接口，用户补值后保存即可修复当前模块跨接口引用。 |
| 本轮 | 变量跨接口改名迁移 | 已补齐基础能力 | `src/components/editor/WorkspaceSettingsView.vue` | 模块变量页新增“跨接口变量改名”，可将当前模块接口 URL、Header、Cookie、Params、Body、Auth 与请求变量中的 `{{旧变量}}` / `{{旧变量:默认值}}` 批量迁移为新变量名，同时改写变量草稿并写入模块审计日志。 |
| 本轮 | Postman 脚本导入导出 | 已补齐基础能力 | `src/utils/import.ts`, `src/utils/export.ts` | Postman Collection v2.1 导入时读取 item 级 `event.listen=prerequest/test` 脚本并映射到接口前置/后置脚本；导出 Postman Collection 时把接口前置/后置脚本写回 `event`，避免 Postman 脚本在迁移中丢失。 |
| 本轮 | Postman 文件夹脚本继承 | 已补齐基础能力 | `src/utils/import.ts` | Postman Collection 导入时会沿文件夹层级继承 folder 级 `prerequest/test` 脚本，并与 item 级脚本合并到导入接口的前置/后置脚本中，避免文件夹共享脚本在扁平化导入时丢失。 |
| 本轮 | Postman 认证导入导出 | 已补齐基础能力 | `src/utils/import.ts`, `src/utils/export.ts` | Postman Collection v2.1 导入/导出补齐 request 级 bearer、basic、apikey 认证映射，避免接口迁移到/从 Postman 时丢失 Auth Tab 配置。 |
| 本轮 | Postman Query/Cookie 导入导出 | 已补齐基础能力 | `src/utils/import.ts`, `src/utils/export.ts` | Postman URL 对象里的 `query` 会导入到 Params 编辑器并从 raw URL 中去重，request `cookie` 会导入到 Cookies 编辑器；导出 Postman Collection 时同步写出启用的 Params 和 Cookies。 |
| 本轮 | Postman Collection Variables 导入导出 | 已补齐基础能力 | `src/utils/import.ts`, `src/utils/export.ts` | Postman Collection 顶层 `variable` 会导入为请求变量，folder/item 级 `variable` 会按层级继承且子级覆盖父级；导出 Postman Collection 时聚合启用的请求变量并写回顶层 `variable`，避免集合变量迁移丢失。 |
| 本轮 | URL 基础地址变量下拉 | 已补齐基础能力 | `src/components/editor/RequestBar.vue` | URL 输入区会从当前环境/模块/请求变量中筛选 `base/url/host/origin/endpoint/api` 或 HTTP(S) 值，提供“基础地址”下拉；选择后以 `{{变量名}}` 替换 URL 前缀并保留当前路径/Query/Hash，对齐设计稿基础地址下拉。 |
| 本轮 | URL 变量内联高亮 | 已补齐基础能力 | `src/components/editor/RequestBar.vue` | URL 输入框新增透明输入层 + 高亮渲染层：已解析的 `{{变量}}` 以主色虚线标记，未解析变量以错误色提示，同时保留原生输入、光标、滚动与 `{{` 自动补全。 |
| 既有 | Category → Module → Interface 双层导航 | 已实现 | `src/stores/workspace.ts`, `src/components/sidebar/Sidebar.vue` | 左侧已按大类/模块/接口渲染，保留旧 `apis/groups` 兼容层。 |
| 既有 | 模块主页/模块变量/设置 | 部分实现 | `src/components/editor/WorkspaceSettingsView.vue` | 有统计卡片、模块类型切换、变量远程/本地值、基础设置、数据源记录、导出/备份。 |
| 既有 | 请求编辑主流程 | 已实现 | `src/components/editor/RequestBar.vue`, `src/components/editor/TabPanel.vue`, `src/utils/http.ts` | Method/URL/Params/Body/Headers/Cookies/Auth/脚本/发送/历史闭环可用。 |
| 既有 | 响应展示 | 已补齐主要功能 | `src/components/response/ResponsePanel.vue`, `src/components/common/JsonTreeViewer.vue`, `src/utils/binary-response.ts` | 状态/耗时/大小、Body Pretty/Raw/Tree/Table/Preview、Headers、Cookie、Console、Visualize、Diff、搜索/过滤、实际请求、复制/保存响应、Markdown 文档与单文件 HTML 响应报告、二进制 base64 保真预览/保存已实现。 |
| 既有 | 导入导出 | 部分实现 | `src/utils/import.ts`, `src/utils/openapi-import.ts`, `src/utils/export.ts`, `src/components/common/ExportPanel.vue` | cURL、Postman、OpenAPI JSON/YAML、HAR 导入，且全屏页支持拖拽文件自动识别导入；cURL/Postman/OpenAPI/Markdown/HTML/代码生成导出；Postman item/folder 级前置/测试脚本、request 级认证、Query、Cookie、Collection Variables 与 folder/item 变量继承已可导入/导出基础映射；OpenAPI Cookie 参数也已支持往返。复杂 YAML 特性仍需补强。 |
| 既有 | 浏览器特有入口 | 部分实现 | `extension/manifest.json`, `extension/background.js`, `extension/content.js`, `extension/devtools-panel.js` | Side Panel、DevTools、contextMenus、commands、webRequest 捕获基础已具备；Manifest 已补齐 32px icon 与 module service worker 声明。 |

## design-doc.md 设计项覆盖清单

| 章节/设计项 | 当前状态 | 证据 | 未实现/待补齐 |
|---|---|---|---|
| 1.2 无工作空间，大类→模块 | 已补齐主要功能 | `Category/Module/InterfaceNode` 类型和 store 已存在，`Sidebar.vue` | 旧 `Group/ApiConfig` 兼容层仍用于兼容请求编辑核心；InterfaceNode folder/request/parentId、基础拖拽移动、文件夹批量发送、文件夹前置脚本与文件夹设置弹窗已补齐主要路径。 |
| 1.3 Popup / Side Panel / 全屏页 / 内容脚本 | 部分实现 | `popup.html`, `sidepanel.html`, `main.html`, `extension/content.js` | Popup 已增强，模块卡片可进入快速发送并预填首个请求或模块 baseUrl 变量，也可把当前模块/接口上下文带到 SidePanel/全屏页，顶部搜索可跨模块查接口并回车发送首条；响应 Body 已改为折叠预览且历史项可一键到侧栏/全屏复用；SidePanel 已有专属头部/捕获列表，展开全屏也会恢复当前上下文；内容脚本导入已可消费页面/选中 JSON，并把 URL、标题、referrer、语言、视口和 frame 信息携带为请求级变量；页面选中 JSON 富格式化/复制/待导入已补基础能力，右键“发送选中 JSON 到 Side Panel 格式化”会自动打开侧栏并粘贴到请求 Body。 |
| 2 UI 设计规范 | 部分实现 | `src/assets/styles/theme.css`, `global.css`, 各 SFC 样式 | 左侧 260px 默认 / 200–400px 拖拽调整 / 48px 折叠、右侧 300px 面板与 0–400px 拖拽调整、主色/暗色/圆角/网格已覆盖；三视场已统一应用 Light/Dark/System 主题并默认跟随系统；URL 变量已具备内联高亮/未解析提示；请求行 Method 彩色圆点与发送 hover 缩放已补；Monaco 未采用，当前用 CodeMirror；更细的动效仍可打磨。 |
| 3 数据模型 | 部分实现 | `src/types/index.ts`, `src/db/index.ts`, `src/stores/workspace.ts`, `WorkspaceSettingsView.vue` | `Module.stats/exportConfig/meta/moduleType/openapiText` 已入模并在 workspace 默认值/设置页中落地；`InterfaceNode` 已支持 folder/request + parentId + preScript/postScript/exportConfig 别名；文档/模型/单接口用例/场景用例已具备独立 Dexie 表与基础管理 UI；单接口和场景用例可运行并回写报告，场景步骤支持局部变量传递与失败继续策略。已补模块本地审计日志表、细粒度权限矩阵和常用断言 DSL，已补远端较新字段级冲突预览/覆盖决策基础，并新增字段级选择合并后写回远端备份；已补本地三视图协同光标 presence，完整 CRDT 式多人合并仍非本地轻量范围。 |
| 4.1 左侧导航交互 | 已补齐主要交互 | `Sidebar.vue`, `WorkspaceSettingsView.vue` | 右键菜单基础、文件夹/子文件夹/文件夹内请求、接口与模块行 hover 快捷发送/复制 cURL、模块右键移动到大类、接口/文件夹/模块拖拽移动、拖拽落点提示、双击设置和确认式删除已补；文件夹/模块均可批量发送（串行/并行）并导出 Markdown 报告；接口右键基准对比已可并排查看配置差异；模块概览已补 Header/Query 批量编辑基础。 |
| 4.2 模块概览页 | 部分实现 | `WorkspaceSettingsView.vue`, `RequestBar.vue` | 接口/文档/模型/单接口用例/场景用例数量与覆盖率已接入真实表数据；头部分享/复制链接按钮、近 7 日调试趋势图和分享/OpenAPI/测试报告/代码生成快捷侧栏已补，并可导出单文件 HTML 文档站。趋势图 hover 详情已补基础能力，OpenAPI 分享已补 `x-apifix-share` 权限元数据、本地片段权限分享链接、权限元数据落地、7 天本地过期校验及接收导入闭环，仍缺服务端权限校验。 |
| 4.3 模块变量页 | 已补齐主要功能 | `WorkspaceSettingsView.vue`, `TabPanel.vue`, `useVariableAutocomplete.ts`, `app.getEnvVariables`, `template.ts` | 模块变量远程/本地值、跨模块 `{{模块名.varName}}` 解析、请求级变量 UI 与请求级最高优先级已补；`{{` 自动补全已展示请求/模块/环境/跨模块/动态函数来源；模块变量支持 JSON 文件批量导入/导出，并新增未解析变量引用静态提示。已补一键把缺失引用批量补到模块变量草稿，以及当前模块跨接口变量改名迁移基础。 |
| 4.4 请求编辑区 | 已补齐主要功能 | `RequestBar.vue`, `BodyEditor.vue`, `TabPanel.vue` | Method 下拉选项彩色圆点、发送按钮 hover 缩放、发送下拉“发送并下载/发送后生成代码”、脚本 Tab 运行/重置/片段/日志、Body 动态值插入、JSON 美化/压缩和行数/字符数已补；GET/HEAD 默认禁用 Body。 |
| 4.5 响应展示区 | 已补齐主要功能 | `ResponsePanel.vue`, `http.ts`, `extension/background.js`, `binary-response.ts` | Cookie tab、实际请求 tab、保存响应、生成 Markdown 文档、脚本测试报告 Tab、Visualize、Preview、Table、Diff、响应搜索/过滤、状态栏 Content-Type 摘要、按内容自动选择默认 Body 模式、图片/PDF 等二进制 base64 保真预览/保存已补；已补单文件 HTML 响应报告模板。 |
| 5.1 模块类型系统 | 部分实现 | `WorkspaceSettingsView.vue`, `openapi-import.ts`, `export.ts`, readonly 锁定 | OpenAPI 文本编辑、JSON/常见 YAML 预览已补，并增强 flow/anchor/merge、数组项块标量、组合 schema、Swagger 2.0 参数解析、Path 参数请求变量与 Security→Auth 映射；已支持从当前可视化接口生成 OpenAPI YAML/JSON，并可将编辑器 OpenAPI 文本增量同步回当前模块。仍缺原文注释/字段顺序/扩展字段无损保留与完整 YAML 1.2 兼容。 |
| 5.2 数据源绑定 | 部分实现 | `WorkspaceSettingsView.vue`, `data-source-sync.ts`, `DataSourceAutoSyncBridge.vue`, `http.ts`, `openapi-import.ts`, `export.ts`, `extension/background.js` | 已支持 OpenAPI/Swagger JSON/增强 YAML 手动拉取、Swagger UI/HTML 规格地址发现、同步日志、外部 `$ref` 常见组件合并与远程根 JSON Schema/JSON Pointer 引用本地化、原始/合并文本保存、接口增量新增/更新、字段映射到接口名称/描述/文件夹分类、按分钟间隔自动同步、同步状态记录和扩展 runtime/external 消息触发 webhook 同步；Webhook 触发密钥已补基础权限校验，本地 `$ref` 示例生成已具备循环保护，常见 `const/examples/nullable/type[]/additionalProperties/min*/max*/prefixItems/minProperties/maxProperties/format/pattern/readOnly/multipleOf` 示例关键字已补并覆盖普通/schema/content 参数示例；完整 YAML 1.2 与所有 JSON Schema 方言关键字无损语义仍缺。 |
| 5.3 请求管理 | 已补齐主要功能 | `Sidebar.vue`, `WorkspaceSettingsView.vue`, `RequestBar.vue`, `TabPanel.vue`, `CodeGenPanel.vue`, import/export utils, `HistoryPanel.vue` | 新建请求/导入/历史星标、无限层级文件夹、文件夹前置脚本、基础拖拽排序/跨模块移动已做；文件夹和模块均可右键串行/并行批量发送并导出 Markdown 测试报告；接口右键可选基准并与另一接口对比配置差异；模块概览可批量新增/覆盖/替换/删除 Header 与 Query 参数；代码生成面板已含 fetch/axios/Python/Java 并保留 Cookies，模块概览快捷侧栏可直达首个接口代码生成，请求级变量 Tab、URL 基础地址变量下拉、URL 变量内联高亮和当前模块变量跨接口改名迁移已补。 |
| 5.4 响应处理 | 已补齐主要功能 | `JsonTreeViewer.vue`, `ResponsePanel.vue`, `CodeGenPanel.vue`, `binary-response.ts`, export utils | Visualizer、JSON 表格、HTML/SVG/图片/PDF Preview、两次响应 Diff、二进制保真保存/下载、请求+响应 Markdown 文档生成入口已补。 |
| 5.5 浏览器特有功能 | 部分实现 | `extension/*`, `http.ts` cookie 开关, `SidePanelView.vue`, `Sidebar.vue`, `content.js` | 全屏页网络模式下拉已绑定真实请求设置，代理模式可直接配置代理 URL；扩展端已用 chrome.cookies 注入自动携带 Cookie，Network 捕获导入已打通；Side Panel 捕获项支持复制/拖拽 cURL；接口树请求/文件夹可拖到当前网页输入区域或派发页面事件；右键选中 JSON 可在页面内格式化、复制，并可通过“发送选中 JSON 到 Side Panel 格式化”自动打开侧栏粘贴到 Body。已补多标签目标选择、页面侧 drop-ready/accept 富协议、Shadow DOM/受控输入拖拽插入、普通 iframe/about:blank frame 注入、CodeMirror/Monaco/ProseMirror/Quill 等低代码编辑器 fallback 和富格式化基础，仍需更多站点专项验证。 |
| 6 脚本引擎 API | 部分实现 | `src/utils/pre-request.ts`, `src/components/editor/RequestBar.vue`, `ResponsePanel.vue`, `public/sandbox.html`, `extension/sandbox.html`, `extension/script-worker.js` | 主流程已支持 async/await、`pm.sendRequest`（含 timeout/followRedirects）、`sendInterface`、Visualizer、结构化测试报告、断言日志、`pm.test.skip`、`pm.info`、Worker sandbox 硬超时、可用时 128MB 增量内存守卫、常用安全全局白名单、`CryptoJS.SHA256`、`pm.environment/globals/collectionVariables/variables/iterationData/collection` 基础作用域，以及响应对象 `size/xml/blob` 常用方法。已补常用 `pm.request` 修改 API（含 `method/url/body` 读写、URL 字符串兼容代理、对象/FormData Body 归一化与读取接口、Header add/set 语义与 `headers['Key']` 下标读写）、请求 Cookie 脚本读写、`pm.cookies` / `pm.cookieJar` 基础 CookieJar、`pm.vault` 本次执行内存兼容层、`pm.execution.skipRequest/setNextRequest/runRequest/location` 基础能力、`pm.request.setHeader` 设计示例别名、响应 headers 下标读取、`match()` 提取返回值、`jsonBody/bodyContains` 响应断言、VariableScope 指定作用域写入、Visualizer 数据优先表格签名、`{{}}`/`<%= %>` 模板插值和 Chai 风格 `pm.expect` 常用链式断言子集/别名，仍缺跨浏览器精确内存计量、安全持久化 Vault、OAuth helper 与完整 Postman Runtime API 兼容。 |
| 7 三视场布局差异 | 部分实现 | `PopupView.vue`, `SidePanelView.vue`, `MainView.vue`, `Sidebar.vue`, `content.js` | Popup 已增强并可搜索/快速发送跨模块接口、模块卡片预填首个请求或 baseUrl、携带当前模块/接口上下文打开 Side Panel/全屏页、折叠响应预览并从历史项直接复用请求；Side Panel 已有专属头部、导航折叠、捕获列表、宽度断点自适应、选中 JSON 自动粘贴 Body 和接口树拖到网页输入区/页面事件，展开全屏会保留当前上下文；全屏页已补 300px 右侧面板、0–400px 拖拽调整、环境/动态值/接口信息与隐藏快捷键，文件夹批量发送/报告入口可在完整页使用，并新增当前模块只读文档视图；全屏页可拖拽 cURL/Postman/OpenAPI/HAR 文件到窗口导入。已补多标签拖拽目标选择基础、页面自定义接收协议、Shadow DOM/受控输入插入、iframe 注入和常见 Web 编辑器插入 fallback。 |
| 7.5 三视场状态同步 | 部分实现 | Pinia/IndexedDB + `PendingImportBridge` + `CrossViewSyncBridge` | 已通过 BroadcastChannel 与 `chrome.runtime` 中继同步环境变量、模块/接口树、设置、API 配置；Popup/SidePanel/全屏页可消费 pending import、`#apifix-share` 分享链接和短时打开上下文；同 scope 并发修改会提示冲突并按 IndexedDB 最新状态刷新，当前接口/Tab 编辑活动和字段/选区行列号会在其他视图以协同光标浮层提示。完整 CRDT 式多人同文档合并仍非本地轻量范围。 |
| 7.6 快捷键 | 已补齐主要功能 | `extension/manifest.json`, `background.js`, `useKeyboardShortcuts.ts`, `shortcuts.ts`, `MainView.vue`, `RequestBar.vue`, `GlobalSearch.vue`, `PopupView.vue`, `SidePanelView.vue` | Ctrl/Cmd+N 新建请求、Ctrl/Cmd+Enter 发送、Ctrl/Cmd+K 搜索接口/环境变量/模块变量/请求变量/历史、搜索 Enter 发送首条结果、Ctrl+Shift+A 打开/聚焦 Popup、Ctrl+Shift+S 打开 Side Panel、Ctrl+Shift+F 打开/聚焦全屏页、Popup Esc 关闭、Side Panel Esc 关闭捕获浮层/折叠导航已补；全屏页新增本地自定义快捷键设置，可重绑定新建、发送、保存、搜索、主题、右栏、历史和文档视图。 |
| 8 IndexedDB 存储 | 部分实现 | `src/db/index.ts`, `WorkspaceSettingsView.vue` | Dexie 表存在；history 已补 moduleId/interfaceId 及组合索引并对旧数据升级回填；本地文件备份、GitHub Gist 备份和 WebDAV PUT 备份已补；Gist/WebDAV 写入前具备远端较新冲突检测、Viewer 写入拦截、覆盖策略和模块变量 local 值脱敏，变量保存/导入可自动触发远端备份。已补本地审计日志和本地细粒度权限基础，已补远端较新字段级冲突预览/覆盖决策基础，并新增字段级选择合并后写回远端备份；已补本地三视图协同光标 presence，完整 CRDT 式多人合并仍非本地轻量范围。 |
| 9 技术实现建议 | 部分实现 | Vue3+TS+Vite+Pinia+Dexie+CodeMirror | Manifest V3 清单已补 32px icon 与 module service worker，主题默认跟随系统；CRXJS 未接入、Monaco 未接入、Headless UI 未引入（当前坚持无新增依赖）。 |
| 10 阶段规划 | 部分实现 | 当前代码 | MVP/V1 多数主流程具备，首次空库会自动创建 JSONPlaceholder/天气 API 示例项目以满足零配置体验；V1.5/V2.0 高级能力仍需继续补齐。 |

## 当前仍未完全实现 / 需额外条件

| 设计项 | 当前状态 | 未完成原因 / 下一步条件 |
|---|---|---|
| 完整 YAML 1.2 与 OpenAPI 原文无损 | 部分实现 | 当前为无新增依赖的轻量解析器，已覆盖常见 OpenAPI YAML、flow/anchor/merge/块标量等；若要声明完整 YAML 1.2、注释/字段顺序/扩展字段无损，需要引入成熟 YAML AST 依赖或专门原文保真层。 |
| 所有 JSON Schema 方言关键字无损语义 | 部分实现 | 已支持常见 schema 示例生成、本地/远程 `$ref`、组合 schema 基础，以及 `const/examples/nullable/type[]/additionalProperties/min*/max*/prefixItems/minProperties/maxProperties/format/pattern/readOnly/multipleOf` 等常用关键字；draft 差异、条件关键字、动态引用等完整语义需要 schema 解释器或校验库。 |
| 完整 Postman Runtime API | 部分实现 | 已补常用 pm.request/response/expect/variables/sendRequest/visualizer、CookieJar、本次执行内存型 `pm.vault` 与 `pm.execution` 基础能力；完整 Runtime 的安全持久化 Vault、OAuth helper、完整 collection runner 生态等仍需要系统性兼容层与测试套件。 |
| 跨浏览器精确脚本内存计量 | 部分实现 | Chromium `performance.memory` 仅为近似指标且非跨浏览器标准；完整能力需要浏览器/运行时暴露可靠内存 API 或独立进程沙箱。 |
| 服务端权限校验 / 团队多人权限 | 本地基础实现 | 当前仅能做本地分享元数据、过期校验、Viewer 写入拦截和备份冲突处理；真正权限校验需要后端身份、授权与审计服务。 |
| 完整 CRDT 多人同文档合并 | 未作为本地轻量能力实现 | 已补三视图状态广播、冲突提示、协同光标 presence；真正 CRDT 需要协同协议、服务端/对等同步与冲突模型。 |
| CRXJS / Monaco / Headless UI | 未引入 | 当前遵循“无明确要求不新增依赖”，仍使用 Vite 自定义扩展打包、CodeMirror/原生组件；若必须完全对齐技术建议，需要明确批准新增依赖和迁移成本。 |
| 复杂低代码站点拖拽适配验证 | 部分实现 | 已补 Shadow DOM、iframe、CodeMirror/Monaco/ProseMirror/Quill fallback；仍需要针对具体第三方站点做人工/浏览器矩阵验证。 |

## 下一步建议（按收益/风险排序）

1. 脚本引擎：继续补更多 Postman Runtime API 兼容；内存守卫已具备 Chromium 近似增量防护，跨浏览器精确计量仍待底层能力。
2. 数据源：字段映射与远程根 JSON Schema/JSON Pointer 引用本地化已补基础；继续补完整 YAML 1.2 与所有 JSON Schema 方言关键字无损语义。
3. 团队/协作：本地审计日志、细粒度权限、备份冲突预览、字段级选择合并写回与三视图协同光标 presence 已补基础；完整 CRDT 多人同文档合并需服务端/协议支撑。

## 验证记录

- 本轮增量验证：`npm run typecheck`、`npm run build`、`npm run build:ext`、`node --check extension/script-worker.js`、提取 `public/sandbox.html` / `extension/sandbox.html` 内联脚本后执行 `node --check`、`test -f dist-extension/script-worker.js`、`test -f dist-extension/icons/icon32.png`、`git diff --check` 均通过；覆盖 Postman CookieJar、Postman Vault 执行内存兼容层、Postman Execution 基础能力、OpenAPI OAuth/OpenID Security 降级映射与 JSON Schema 参数/请求体示例常见关键字（含 readOnly/max* 约束）、OpenAPI 参数 content 示例、`pm.request.headers` 下标读写与 `pm.request.body` 对象/FormData/Record KV 写入桥接与读取接口、`pm.sendRequest({ body })` 同步归一化与 `pm.request.url` 字符串兼容代理补齐；构建仅保留 Vite 大 chunk 警告。
- `node --check extension/background.js && node --check extension/content.js && node --check extension/script-worker.js && node --check extension/icons/generate.js && python3 sandbox-script-check && npm run build && npm run build:ext && test -f dist-extension/script-worker.js && test -f dist-extension/icons/icon32.png && git diff --check`：已通过（包含 OpenAPI 模块双向同步、脚本 Postman 变量作用域/CryptoJS、模块概览趋势/快捷侧栏/代码生成入口、场景局部变量与失败继续策略、请求级变量/URL 基础地址变量下拉/URL 变量内联高亮/跨作用域补全/模块变量文件导入导出/变量引用静态校验、文件夹批量发送/Markdown 报告、文件夹设置页、接口配置对比、全屏页文档视图、变量保存前校验、HTML 文档站导出、复杂 JSONPath 断言/提取、Side Panel 多标签拖拽目标选择/复杂输入适配/iframe 注入覆盖、低代码编辑器拖拽插入 fallback、页面上下文元数据携带、Ctrl/Cmd+N 新建请求、全局搜索覆盖模块/请求变量、全局快捷键打开/聚焦 Popup 与全屏页、Popup 跨模块搜索回车发送/模块卡片预填 baseUrl/响应折叠预览/历史复用/状态规则、Body JSON 美化压缩、请求行 Method 彩色圆点/发送 hover、响应状态栏/默认 Body 模式、零配置示例项目、三视场系统主题、网络模式真实生效/代理 URL 配置、Manifest 图标与 module service worker 对齐、Side Panel 宽度断点自适应、History 模块/接口索引对齐、模块变量远端备份隐私与自动同步、数据源字段映射应用、远程 JSON Schema `$ref` 本地化、左侧导航拖拽调整、全屏页右侧面板拖拽调整、全屏页拖拽文件导入、Popup/SidePanel 打开上下文恢复、跨视图编辑活动提示/协同光标 presence、模块本地审计日志、用户自定义快捷键、场景逐步骤策略 UI、变量缺失批量补齐、变量跨接口改名迁移、测试断言 DSL 扩展、页面侧请求拖拽富协议、测试变量可视化提取器、Postman 请求 API/Cookie 脚本 API/响应 JSON 断言常用方法扩展、Chai 断言链常用别名补强、模块细粒度权限矩阵、模块趋势 hover 细节、备份冲突预览决策、响应 HTML 报告模板、OpenAPI Security Auth/Path/Cookie 参数导入导出、OpenAPI 权限化分享元数据/本地权限分享链接/权限元数据落地/7 天过期校验/接收导入、三视图同步冲突提示、请求管理批量 Header/Query 编辑、代码生成与文档 Cookies 保真、YAML 数组项块标量解析、OpenAPI 本地 `$ref` 循环保护、脚本 Worker 内存守卫、Postman 变量作用域 import 方法、Postman 脚本导入导出、Postman 文件夹脚本继承、Postman 认证导入导出、Postman Query/Cookie/Collection Variables/folder-item 变量继承导入导出、页面选中 JSON 富格式化/自动粘贴 Body、模块行 hover 快捷发送/复制/移动到大类、模块主页分享按钮、数据源 Webhook 触发密钥、备份字段级冲突预览/合并写回、Postman 响应对象常用方法补齐、Postman 响应 headers/match 兼容、Chai 风格断言链补强、Postman 请求/集合别名补齐、pm.request.method/url/body 改写、结构化脚本测试报告、脚本编辑器重置、脚本日志毫秒格式、ScriptTimeoutError 超时类型、Header add/set 语义、setPathParam 模板占位、pm.sendRequest timeout/followRedirects、pm.request 顶层 Query/Path 方法、pm.test.skip、Visualizer 表格签名对齐、Visualizer EJS 插值、变量 set 指定作用域、pm.info 脚本上下文等本轮新增补齐项；`npm run build` 内含 `vue-tsc -b`，`build:ext` 已确认复制 `script-worker.js`）。Vite 仅保留大 chunk 警告。
- 已按要求在验证前后执行 `git checkout -- tsconfig.app.tsbuildinfo 2>/dev/null || true`，避免提交 typecheck 产物。
