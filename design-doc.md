# API Fox Lite — 浏览器插件设计文档

> 版本：v1.0  
> 日期：2026-05-15  
> 定位：参照 Apifox 网页端设计风格，开发一套无工作空间概念的简易 API 管理浏览器插件

---

## 目录

1. [产品定位与架构](#一产品定位与架构)
2. [UI 设计规范](#二ui-设计规范)
3. [数据模型：大类与模块](#三数据模型大类与模块)
4. [页面结构与交互设计](#四页面结构与交互设计)
5. [核心功能模块](#五核心功能模块)
6. [脚本引擎 API 设计](#六脚本引擎-api-设计)
7. [三视场布局差异](#七三视场布局差异)
8. [数据存储方案](#八数据存储方案)
9. [技术实现建议](#九技术实现建议)
10. [开发阶段规划](#十开发阶段规划)

---

## 一、产品定位与架构

### 1.1 产品定位

轻量级 API 调试与管理插件，主打「开箱即用、随开随调」，覆盖 80% 日常接口调试场景，复杂协作场景仍引导至云端/桌面端。

### 1.2 核心架构变更：无工作空间

摒弃传统「工作空间/Workspace」隔离项目的模式，采用 **「大类（Category）→ 模块（Module）」** 的二级扁平结构：

```
┌─────────────────────────────────────────┐
│            应用级全局配置                 │
│  (主题、快捷键、代理、数据备份策略)        │
├─────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ 大类 A  │  │ 大类 B  │  │ 大类 C  │ │
│  │ (电商)  │  │ (政务)  │  │ (内部)  │ │
│  └────┬────┘  └────┬────┘  └────┬────┘ │
│       │            │            │      │
│  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐│
│  │模块A-1  │  │模块B-1  │  │模块C-1  ││
│  │(订单)   │  │(一件事) │  │(用户)   ││
│  │[通用API]│  │[通用API]│  │[通用API]││
│  │[YAML]   │  │[YAML]   │  │[YAML]   ││
│  │[只读]   │  │[只读]   │  │[只读]   ││
│  └─────────┘  └─────────┘  └─────────┘│
│  ┌─────────┐  ┌─────────┐            │
│  │模块A-2  │  │模块B-2  │            │
│  │(支付)   │  │(查询)   │            │
│  └─────────┘  └─────────┘            │
└─────────────────────────────────────────┘
```

**关键原则**：
- **无工作空间**：所有大类平级共存，左侧导航直接展示大类列表
- **模块即项目**：模块是功能完整的最小单元，具备概览、变量、设置、脚本等完整能力
- **跨模块引用**：环境变量支持跨模块引用（如 `{{模块B.token}}`）

### 1.3 运行形态

| 模式 | 入口 | 适用场景 |
|------|------|----------|
| **Popup 浮窗** | 点击工具栏图标 | 快速发起单个请求、查看历史 |
| **Side Panel 侧边栏** | 右键「打开侧边栏」或快捷键 | 分屏调试，与网页开发并行 |
| **独立全屏页** | `chrome-extension://id/index.html` | 复杂项目、批量测试、文档生成 |
| **内容脚本注入** | 在网页右键菜单捕获请求 | 一键导入浏览器 Network 请求 |

---

## 二、UI 设计规范

### 2.1 色彩体系（Light 模式为主，支持 Dark）

```css
:root {
  /* 主色 */
  --primary: #6366f1;           /* Indigo-500，Apifox 紫蓝风格 */
  --primary-hover: #4f46e5;     /* Indigo-600 */
  --primary-light: #e0e7ff;     /* Indigo-50 */

  /* 背景 */
  --bg-base: #ffffff;
  --bg-sidebar: #f8f9fa;        /* 左侧导航栏 */
  --bg-panel: #ffffff;            /* 主内容区 */
  --bg-code: #f4f5f7;             /* JSON 编辑器背景 */

  /* 边框与分割线 */
  --border: #e5e7eb;              /* Gray-200 */
  --divider: #f3f4f6;             /* Gray-100 */

  /* 文字 */
  --text-primary: #111827;        /* Gray-900 */
  --text-secondary: #6b7280;      /* Gray-500 */
  --text-tertiary: #9ca3af;       /* Gray-400 */

  /* 状态色 */
  --success: #10b981;             /* 200 绿色 */
  --warning: #f59e0b;             /* 400 黄色 */
  --error: #ef4444;               /* 500 红色 */
  --info: #3b82f6;                /* GET 蓝色 */

  /* 方法标签色 */
  --method-get: #3b82f6;
  --method-post: #10b981;
  --method-put: #f59e0b;
  --method-delete: #ef4444;
  --method-patch: #8b5cf6;
}
```

### 2.2 字体与排版

- **字体栈**：`Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`
- **代码字体**：`JetBrains Mono, Consolas, monospace`
- **字号层级**：
  - 标题：14px / 600 weight
  - 正文：13px / 400 weight
  - 代码：12px
  - 辅助说明：11px

### 2.3 布局网格

- **左侧边栏**：宽度 260px，可折叠至 48px（仅图标）
- **主内容区**：弹性填充，最小宽度 600px
- **右侧面板**（可选）：300px，用于环境变量/动态值
- **标签页高度**：36px
- **行高/列表项**：32px
- **圆角**：按钮 6px，卡片 8px，输入框 4px

---

## 三、数据模型：大类与模块

### 3.1 大类（Category）

```typescript
interface Category {
  id: string;              // nanoid
  name: string;             // 如：数字空间、电商系统
  color: string;            // #6366f1 等，用于左侧标签
  sortOrder: number;
  createdAt: number;
  updatedAt: number;
}
```

### 3.2 模块（Module）— 核心单元

```typescript
interface Module {
  id: string;
  categoryId: string;
  name: string;             // 如：残疾人服务一件事
  type: 'generic' | 'openapi-yaml' | 'readonly';

  // 概览统计
  stats: {
    interfaceCount: number;
    docCount: number;
    modelCount: number;
    testCaseTotal: number;
    testCaseCoverage: number;
    sceneCaseTotal: number;
    sceneCaseCoverage: number;
    avgCasePerInterface: number;
    uncoveredInterfaceCount: number;
  };

  // 模块变量（远程值 + 本地值）
  variables: {
    remote: Record<string, string>;   // 团队共享
    local: Record<string, string>;    // 本地覆盖
  };

  // 数据源配置
  dataSource?: {
    type: 'swagger' | 'openapi' | 'custom';
    url: string;
    syncStrategy: 'manual' | 'auto' | 'webhook';
    fieldMapping: Record<string, string>;
    lastSyncAt: number;
  };

  // 模块类型配置
  moduleType: {
    mode: 'visual' | 'yaml' | 'readonly';
    description: string;
  };

  // 接口列表（树形结构）
  interfaces: InterfaceNode[];

  // 导出/备份配置
  exportConfig: {
    format: 'openapi3' | 'markdown' | 'html';
    autoBackup: boolean;
    backupTarget?: 'gist' | 'webdav' | 'local';
  };

  meta: {
    createdAt: number;
    updatedAt: number;
    version: string;
  };
}
```

### 3.3 接口节点（树形）

```typescript
interface InterfaceNode {
  id: string;
  type: 'folder' | 'request';
  parentId: string | null;
  name: string;
  sortOrder: number;

  // 仅 request 类型有：
  method?: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH' | 'HEAD' | 'OPTIONS';
  url?: string;
  params?: Param[];
  headers?: Header[];
  body?: BodyConfig;
  auth?: AuthConfig;
  preScript?: string;
  postScript?: string;

  children?: InterfaceNode[];  // folder 类型
}

interface Param {
  key: string;
  value: string;
  description?: string;
  enabled: boolean;
}

interface Header {
  key: string;
  value: string;
  description?: string;
  enabled: boolean;
}

interface BodyConfig {
  mode: 'none' | 'form-data' | 'urlencoded' | 'json' | 'xml' | 'text' | 'binary';
  content: string | FormDataItem[];
}

interface AuthConfig {
  type: 'none' | 'bearer' | 'basic' | 'apikey';
  config: Record<string, string>;
}
```

---

## 四、页面结构与交互设计

### 4.1 全局导航（左侧边栏 — 双层结构）

```
┌─────────────────────────────────┐
│  🔍 搜索接口 / 模块 / 大类...    │
├─────────────────────────────────┤
│  ▼ 📁 电商系统（大类）           │
│    ├─ ▶ 订单模块                │
│    ├─ ▶ 支付模块                │
│    └─ ▶ 库存模块                │
│                                 │
│  ▼ 📁 数字空间（大类）           │
│    ├─ ▶ 残疾人服务一件事         │  ← 当前选中（高亮）
│    │   ├─ GET /api/init         │
│    │   ├─ POST /api/submit      │
│    │   └─ GET /api/query        │
│    ├─ ▶ 公钥同步模块             │
│    └─ ▶ 会话管理模块             │
│                                 │
│  ▼ 📁 内部工具（大类）           │
│    └─ ▶ 用户中心                 │
├─────────────────────────────────┤
│  [+] 新建大类                    │
│  ⚙️ 环境变量  │  🗑️ 回收站       │
└─────────────────────────────────┘
```

**交互细节**：
- **大类行**：点击箭头展开/折叠，双击进入大类设置页
- **模块行**：单击选中，右侧展示模块内容；hover 显示快捷操作（▶ 发送全部、📋 复制、⚙️ 设置）
- **接口行**：hover 显示「发送」小图标（▶ 14px），点击直接执行请求
- **拖拽规则**：模块可跨大类拖拽；接口只能在模块内/模块间拖拽
- **右键菜单**：
  - 大类：重命名 / 修改颜色标签 / 删除（需确认）
  - 模块：设置 / 复制 / 移动到大类 / 删除
  - 接口：发送 / 复制 cURL / 移动到其他模块

### 4.2 模块主页 — 概览页

```
┌─────────────────────────────────────────────────────────────────────┐
│  Master（模块名称）                                    [保存] [分享] │
│  ─────────────────────────────────────────────────────────────────── │
│  [概览 ●] [模块变量] [设置]                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  📊 统计                                                      │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐                      │   │
│  │  │   11    │  │    0    │  │   17    │                      │   │
│  │  │ 接口数   │  │ 文档数   │  │ 数据模型 │                      │   │
│  │  └─────────┘  └─────────┘  └─────────┘                      │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐  │
│  │  📋 单接口用例覆盖            │  │  🔄 场景用例覆盖              │  │
│  │  ┌─────────┐  ┌─────────┐   │  │  ┌─────────┐  ┌─────────┐   │  │
│  │  │   **    │  │   **    │   │  │  │   **    │  │   **    │   │  │
│  │  │用例总数  │  │覆盖率    │   │  │  │用例总数  │  │覆盖率    │   │  │
│  │  └─────────┘  └─────────┘   │  │  └─────────┘  └─────────┘   │  │
│  │  ┌─────────┐  ┌─────────┐   │  │  ┌─────────┐  ┌─────────┐   │  │
│  │  │   **    │  │   **    │   │  │  │   **    │  │   **    │   │  │
│  │  │平均用例数│  │无覆盖接口 │   │  │  │未覆盖接口 │  │...      │   │  │
│  │  └─────────┘  └─────────┘   │  └─────────────────────────────┘  │
│  └─────────────────────────────┘                                   │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  🧩 模块类型                                                   │   │
│  │  ┌─────────┐                                                  │   │
│  │  │[🟦图标]  │  完全可视化：通过可视化方式设计、修改接口         │   │
│  │  │ 通用API │  OpenAPI YAML：直接编辑 YAML/JSON 格式文件        │   │
│  │  └─────────┘  只读模式：禁止手动修改，使用自动导入更新数据      │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  🔗 绑定数据源                                                 │   │
│  │  暂无数据源  [+ 添加数据源]                                     │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  📦 导出/备份 API 规格                                         │   │
│  │  [边聊边办 v1.0.0]  [导出 OpenAPI] [导出 Markdown] [备份到Gist] │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌──────────┐                                                       │
│  │ 快捷操作  │  ├─ 快捷分享                                          │
│  │ 侧边栏   │  ├─ 文档站                                            │
│  │          │  ├─ 生成代码                                          │
│  │          │  ├─ 导入数据                                          │
│  │          │  ├─ 导出数据                                          │
│  │          │  └─ 更多功能设置                                      │
│  └──────────┘                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

**交互细节**：
- 统计卡片 hover 显示趋势图（最近 7 天）
- 模块类型卡片点击切换类型，切换时提示数据可能丢失
- 数据源添加后显示同步状态（最后同步时间、下次同步时间）

### 4.3 模块变量页

```
┌─────────────────────────────────────────────────────────────────────┐
│  Master（模块名称）                                        [保存]      │
│  ─────────────────────────────────────────────────────────────────── │
│  [概览] [模块变量 ●] [设置]                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────┬─────────────────┬─────────────────┐            │
│  │  变量名          │  远程值          │  本地值          │  [?]     │
│  ├─────────────────┼─────────────────┼─────────────────┤            │
│  │  baseUrl         │                 │  localhost:8080 │  [🗑️]   │
│  │  token           │                 │  eyJhbG...      │  [🗑️]   │
│  │  userId          │                 │  440101...      │  [🗑️]   │
│  ├─────────────────┼─────────────────┼─────────────────┤            │
│  │  添加变量...     │                 │                 │            │
│  └─────────────────┴─────────────────┴─────────────────┘            │
│                                                                     │
│  💡 变量使用说明：                                                   │
│  • 远程值：团队协作共享，修改后同步给所有成员                        │
│  • 本地值：仅当前浏览器生效，覆盖远程值，适合本地调试                │
│  • 在 URL/Body/Header 中使用 {{变量名}} 引用                         │
└─────────────────────────────────────────────────────────────────────┘
```

**变量作用域规则**：

| 优先级 | 级别 | 说明 |
|--------|------|------|
| 1 | 请求级变量 | 当前接口内定义的临时变量 |
| 2 | 本地值 | 模块变量页中填写的本地覆盖值 |
| 3 | 远程值 | 模块变量页中的团队共享值 |
| 4 | 全局变量 | 应用级，所有模块共享 |
| 5 | 动态表达式 | `{{$timestamp}}` 等内置函数 |

**跨模块引用语法**：

| 语法 | 说明 | 示例 |
|------|------|------|
| `{{varName}}` | 当前模块变量 | `{{baseUrl}}` |
| `{{模块名.varName}}` | 跨模块引用 | `{{数字空间.token}}` |
| `{{$function}}` | 内置动态函数 | `{{$timestamp}}` |

### 4.4 请求编辑区

#### A. 请求行（顶部固定）

```
┌─────────────────────────────────────────────────────────────┐
│ [POST ▼] [ https://api.example.com/v1/users        ] [发送 ▼] │
│           └─ 基础地址下拉（显示当前环境变量前缀）              │
├─────────────────────────────────────────────────────────────┤
│ [Params] [Body] [Headers] [Cookies] [Auth] [前置] [后置] [设置] │
└─────────────────────────────────────────────────────────────┘
```

**交互细节**：
- **Method 选择器**：下拉菜单，选项带颜色圆点（GET 蓝、POST 绿等）
- **URL 输入框**：支持 `{{` 触发环境变量浮层提示，高亮显示已识别的变量（紫色下划线）
- **发送按钮**：主色填充，hover 缩放 1.02；下拉箭头可选「发送并下载」「发送后生成代码」

#### B. Body 编辑器（JSON 模式）

```
┌─────────────────────────────────────────────────────────────┐
│ [none] [form-data] [x-www-form] [JSON ●] [XML] [Text] [Binary]│
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │  1 │ {                                                  │ │
│ │  2 │   "systemId": "C90-44001697",                      │ │
│ │  3 │   "query": {                                       │ │
│ │  4 │     "svcCode": "bizUrRequest",                     │ │
│ │  5 │     "itemName": "残疾人服务一件事"                  │ │
│ │  6 │   }                                                │ │
│ │  7 │ }                                                  │ │
│ └─────────────────────────────────────────────────────────┘ │
│  [美化] [压缩] [动态值]              字符数: 128  │  行数: 7   │
└─────────────────────────────────────────────────────────────┘
```

#### C. 脚本编辑器（前置/后置操作）

```
┌─────────────────────────────────────────────────────────────────────┐
│  POST 数字空间-发起会话                                    [发送 ▼]  │
│  ─────────────────────────────────────────────────────────────────── │
│  [Params] [Body] [Headers] [Cookies] [Auth] [前置操作 ●] [后置操作] [设置]│
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────┐  ┌───────────────────────────────────────────────┐ │
│  │ Pre-request │  │  1  // 前置脚本：在请求发送前执行              │ │
│  │             │  │  2  // 可用全局对象：pm, console, globals      │ │
│  │ Post-respons│  │  3                                             │ │
│  │    ●        │  │  4  // 示例：自动获取 Token                   │ │
│  │             │  │  5  const res = await pm.sendRequest({        │ │
│  │             │  │  6    url: '{{baseUrl}}/auth/login',           │ │
│  │             │  │  7    method: 'POST',                          │ │
│  │             │  │  8    body: { username: 'admin', pwd: '***' } │ │
│  │             │  │  9  });                                        │ │
│  │             │  │ 10                                             │ │
│  │             │  │ 11  pm.globals.set('token', res.json().token); │ │
│  │             │  │ 12  pm.request.headers.add('Authorization',    │ │
│  │             │  │ 13    'Bearer ' + res.json().token);           │ │
│  │             │  │ 14                                             │ │
│  └─────────────┘  └───────────────────────────────────────────────┘ │
│                                                                     │
│  [运行脚本] [重置] [插入代码片段 ▼]                                   │
│                                                                     │
│  脚本日志：                                                          │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  [14:32:05] 脚本开始执行...                                     │ │
│  │  [14:32:06] 发送请求: POST http://localhost:8080/auth/login    │ │
│  │  [14:32:06] 响应: 200 OK, 耗时 45ms                            │ │
│  │  [14:32:06] 设置全局变量: token = eyJhbGciOiJIUzI1NiIs...      │ │
│  │  [14:32:06] 脚本执行完成 ✓                                      │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.5 响应展示区

```
┌─────────────────────────────────────────────────────────────┐
│ Body │ Cookie │ Header │ 控制台 │ 实际请求          200 │ 652ms │ 918B │
├─────────────────────────────────────────────────────────────┤
│ [Pretty] [Raw] [Preview] [Visualize] [JSON ▼]     [⬇️ 保存] │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │  1 │ {                                                  │ │
│ │  2 │   "errcode": 0,                                    │ │
│ │  3 │   "errmsg": "",                                    │ │
│ │  4 │   "data": {                                        │ │
│ │  5 │     "page": { "total": 0 },                        │ │
│ │  6 │     "realUrl": "https://..."                       │ │
│ │  7 │   }                                                │ │
│ │  8 │ }                                                  │ │
│ └─────────────────────────────────────────────────────────┘ │
│  状态: 成功  │  响应头: application/json  │  [复制响应] [搜索] │
└─────────────────────────────────────────────────────────────┘
```

**状态栏规则**：
- 200-299：绿色；300-399：蓝色；400-499：橙色；500+：红色
- 时间 > 1000ms 时黄色警告，> 3000ms 时红色警告
- 大小自动转换单位（B/KB/MB）

---

## 五、核心功能模块

### 5.1 模块类型系统

| 类型 | 图标 | 说明 | 适用场景 |
|------|------|------|----------|
| **通用 API** | 🟦 | 完全可视化编辑，支持表单 + JSON 双模式 | 常规 REST API 开发 |
| **OpenAPI YAML** | 📄 | 直接编辑 YAML/JSON 文本，实时预览 | 已有 Swagger 文档维护 |
| **只读模式** | 🔒 | 禁止手动修改，仅通过导入/同步更新 | 对接第三方标准接口 |

**类型切换交互**：
- 模块设置页可切换类型，切换时提示「切换后部分数据可能丢失」
- 通用 API ↔ YAML 双向无损转换（基于 OpenAPI 3.0 规范）
- 只读模式切换为其他类型时需确认解锁

### 5.2 数据源绑定

```
┌─────────────────────────────────────────────────────────────┐
│  🔗 绑定数据源                                               │
├─────────────────────────────────────────────────────────────┤
│  当前数据源：Swagger UI (http://api.example.com/swagger-ui)  │
│                                                             │
│  同步策略：                                                  │
│  ○ 手动同步    ● 自动同步（每小时）    ○ 推送同步（Webhook）  │
│                                                             │
│  字段映射：                                                  │
│  ┌─────────────────┬─────────────────┐                       │
│  │  数据源字段      │  模块字段        │                       │
│  ├─────────────────┼─────────────────┤                       │
│  │  operationId   │  接口名称        │                       │
│  │  summary       │  接口描述        │                       │
│  │  tags[0]       │  文件夹分类      │                       │
│  └─────────────────┴─────────────────┘                       │
│                                                             │
│  [立即同步] [查看同步日志] [断开连接]                          │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 请求管理

| 功能 | 说明 |
|------|------|
| **新建请求** | `Ctrl+N`，默认继承当前环境 |
| **导入** | 支持 cURL、Postman Collection v2.1、Swagger/OpenAPI 3.0、HAR 文件 |
| **导出** | 导出为 cURL、Python requests、JavaScript fetch/axios、Postman Collection |
| **历史记录** | 自动保存最近 100 条请求，支持星标收藏 |
| **文件夹** | 支持无限层级，文件夹可设置「前置脚本」（自动给子请求加 Header） |

### 5.4 响应处理

| 功能 | 说明 |
|------|------|
| **可视化** | JSON 转表格、图片预览、PDF 预览、HTML 渲染 |
| **差异对比** | 对比两次响应的 JSON 差异（类似 Git Diff） |
| **生成代码** | 根据请求生成各种语言的 HTTP 请求代码 |
| **生成文档** | Markdown 格式接口文档，含请求/响应示例 |

### 5.5 浏览器特有功能

| 功能 | 说明 |
|------|------|
| **Cookie 自动携带** | 自动带上当前浏览器域名下的 Cookie（可开关） |
| **跨域代理** | 插件内置 CORS 代理，解决本地开发跨域问题（可选） |
| **Network 捕获** | DevTools 面板中一键将浏览器 Network 请求导入插件 |
| **页面上下文** | 在网页中右键选中 JSON，直接「发送到 API Fox Lite 格式化」 |

---

## 六、脚本引擎 API 设计

### 6.1 执行架构

```
┌─────────────────────────────────────────────────────────────┐
│                    主应用进程 (Vue 3)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │  请求编辑器  │  │  响应展示器  │  │     变量存储中心     │ │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘ │
│         │                │                    │            │
│         └────────────────┴────────────────────┘            │
│                           │                                │
│              ┌────────────┴────────────┐                  │
│              │    脚本调度器 (Script     │                  │
│              │     Orchestrator)         │                  │
│              │  • 注入上下文对象           │                  │
│              │  • 捕获日志输出             │                  │
│              │  • 超时强制终止             │                  │
│              └────────────┬────────────┘                  │
│                           │                                │
│         ┌─────────────────┼─────────────────┐              │
│         ▼                 ▼                 ▼              │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐       │
│  │  Pre-request │ │ Post-response│ │  独立 Worker │       │
│  │   沙箱 iframe│ │   沙箱 iframe│ │  (sendRequest│       │
│  │              │ │              │ │   异步任务)   │       │
│  └──────────────┘ └──────────────┘ └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

**核心机制**：
- **沙箱环境**：通过 `iframe` + `srcdoc` 创建隔离执行环境，禁用 `window`、`document`、`eval`、`Function`
- **通信方式**：`postMessage` 双向通信，主进程控制所有 IO 操作
- **异步支持**：脚本支持顶层 `await`，内部通过 Promise 包装
- **超时熔断**：默认 30 秒强制终止，防止死循环

### 6.2 全局对象 `pm` 完整 API

```typescript
// 核心命名空间
interface PM {
  // ═══════════════════════════════════════
  // 1. 请求操作（仅 Pre-request 可用）
  // ═══════════════════════════════════════
  request: {
    /** 获取/设置请求方法 */
    method: string;
    /** 获取/设置完整 URL（含参数） */
    url: string;
    /** 获取/设置请求体 */
    body: string | object | FormData;
    /** 添加请求头（不会覆盖同名） */
    addHeader(key: string, value: string): void;
    /** 设置请求头（覆盖同名） */
    setHeader(key: string, value: string): void;
    /** 移除请求头 */
    removeHeader(key: string): void;
    /** 获取所有请求头 */
    headers: Record<string, string>;
    /** 添加/修改 URL 查询参数 */
    addQueryParam(key: string, value: string): void;
    /** 移除查询参数 */
    removeQueryParam(key: string): void;
    /** 设置 Path 参数 /api/:id → /api/123 */
    setPathParam(key: string, value: string | number): void;
  };

  // ═══════════════════════════════════════
  // 2. 响应操作（仅 Post-response 可用）
  // ═══════════════════════════════════════
  response: {
    /** HTTP 状态码 */
    code: number;
    /** 状态文本 */
    status: string;
    /** 响应时间（ms） */
    responseTime: number;
    /** 响应体大小（bytes） */
    responseSize: number;
    /** 响应头 */
    headers: Record<string, string>;
    /** 响应体文本 */
    text(): string;
    /** 解析为 JSON */
    json(): any;
    /** 解析为 XML（返回 DOM-like 对象） */
    xml(): any;
    /** 按 JSONPath 提取 */
    jsonPath(path: string): any;
    /** 正则提取 */
    match(regex: RegExp | string): string | null;
  };

  // ═══════════════════════════════════════
  // 3. 变量存储（四级作用域）
  // ═══════════════════════════════════════
  globals: VariableScope;      // 全局（跨大类跨模块）
  environment: VariableScope; // 环境（当前模块-远程值）
  variables: VariableScope;    // 本地（当前模块-本地值）
  collection: VariableScope;  // 模块级（当前模块共享）

  // ═══════════════════════════════════════
  // 4. 断言与测试
  // ═══════════════════════════════════════
  test: {
    /** 定义测试用例 */
    (name: string, fn: () => void | Promise<void>): void;
    /** 跳过测试 */
    skip(name: string, fn?: () => void): void;
  };
  expect: ChaiExpect;  // 基于 Chai 的 BDD 断言风格

  // ═══════════════════════════════════════
  // 5. 辅助请求（支持异步）
  // ═══════════════════════════════════════
  sendRequest: {
    /** 发送 HTTP 请求（返回 Promise） */
    (req: RequestConfig | string): Promise<Response>;
    /** 发送当前模块内的其他接口 */
    sendInterface(interfaceId: string, overrides?: object): Promise<Response>;
  };

  // ═══════════════════════════════════════
  // 6. 可视化与输出
  // ═══════════════════════════════════════
  visualizer: {
    /** 用模板渲染响应（EJS / Handlebars） */
    template(templateString: string, data?: object): void;
    /** 设置表格数据 */
    table(data: any[]): void;
  };
  info: {
    /** 当前模块名 */
    moduleName: string;
    /** 当前大类名 */
    categoryName: string;
    /** 当前接口名 */
    interfaceName: string;
    /** 执行阶段 */
    eventName: 'prerequest' | 'test';
  };
}

// 变量作用域接口
interface VariableScope {
  /** 获取变量值（自动解析作用域链） */
  get(key: string): any;
  /** 设置变量（当前作用域） */
  set(key: string, value: any): void;
  /** 设置变量（指定作用域） */
  set(key: string, value: any, scope: 'global' | 'module' | 'local'): void;
  /** 删除变量 */
  unset(key: string): void;
  /** 替换字符串中的变量模板 {{var}} */
  replaceIn(template: string): string;
  /** 导出全部变量 */
  toObject(): Record<string, any>;
  /** 批量导入 */
  import(obj: Record<string, any>): void;
  /** 清空 */
  clear(): void;
}

// 请求配置
interface RequestConfig {
  url: string;           // 支持 {{变量}}
  method?: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH' | 'HEAD' | 'OPTIONS';
  headers?: Record<string, string>;
  body?: string | object | {
    mode: 'raw' | 'json' | 'formdata' | 'urlencoded';
    content: any;
  };
  timeout?: number;      // 默认 30000ms
  followRedirects?: boolean; // 默认 true
}

// 响应对象
interface Response {
  code: number;
  status: string;
  headers: Record<string, string>;
  responseTime: number;
  text(): string;
  json(): any;
  xml(): any;
  blob(): Promise<Blob>;
  size: number;
}
```

### 6.3 断言系统（Chai 风格）

```javascript
// 后置脚本示例
pm.test("接口状态正常", () => {
  pm.expect(pm.response.code).to.equal(200);
});

pm.test("响应结构正确", () => {
  const json = pm.response.json();
  pm.expect(json).to.have.property('errcode', 0);
  pm.expect(json.data).to.have.property('list').that.is.an('array');
});

pm.test("响应时间小于 500ms", () => {
  pm.expect(pm.response.responseTime).to.be.below(500);
});

pm.test("Token 格式校验", () => {
  const token = pm.environment.get('token');
  pm.expect(token).to.match(/^Bearer\s+.+/)<boltAction type="file" filePath="/mnt/agents/output/design-doc.md">;
});

// 异步断言
pm.test("异步数据加载完成", async () => {
  const res = await pm.sendRequest('{{baseUrl}}/status');
  pm.expect(res.json().ready).to.be.true;
});
```

**支持的断言链**：

| 类别 | 示例 |
|------|------|
| 相等 | `.to.equal(val)`, `.to.eql(deepObj)`, `.to.deep.equal(obj)` |
| 类型 | `.to.be.a('string')`, `.to.be.an('array')` |
| 包含 | `.to.include('str')`, `.to.contain.key('id')` |
| 范围 | `.to.be.above(10)`, `.to.be.below(100)`, `.to.be.within(2, 10)` |
| 存在 | `.to.exist`, `.to.be.null`, `.to.be.undefined` |
| 匹配 | `.to.match(/regex/)`, `.to.have.length(5)` |
| 异常 | `.to.throw()`, `.to.not.throw()` |

### 6.4 `pm.sendRequest` 使用示例

```javascript
// 示例 1：登录获取 Token（前置脚本）
const loginRes = await pm.sendRequest({
  url: '{{baseUrl}}/auth/login',
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: {
    mode: 'json',
    content: { username: 'admin', password: pm.globals.get('adminPwd') }
  }
});

pm.globals.set('accessToken', loginRes.json().data.token);
pm.request.setHeader('Authorization', 'Bearer ' + loginRes.json().data.token);

// 示例 2：调用当前模块其他接口
const userRes = await pm.sendRequest.sendInterface('user-get-info', {
  headers: { 'X-Request-From': 'batch-script' }
});

// 示例 3：文件下载（后置脚本）
if (pm.response.headers['content-type']?.includes('pdf')) {
  const blob = await pm.response.blob();
  pm.visualizer.template(`
    <a href="data:application/pdf;base64,<%= data %>" download="report.pdf">
      点击下载 PDF
    </a>
  `, { data: await blobToBase64(blob) });
}
```

### 6.5 执行生命周期

```
┌──────────────┐     ┌──────────────────┐     ┌──────────────┐
│   用户点击    │     │   脚本执行阶段    │     │   结果反馈    │
│   [发送]     │ ──→ │                  │ ──→ │              │
└──────────────┘     ├──────────────────┤     └──────────────┘
                     │ 1. 构建请求对象    │              ▲
                     │ 2. 解析变量 {{}}   │              │
                     │ 3. [Pre-request]   │              │
                     │    • 修改请求头    │              │
                     │    • 设置变量      │              │
                     │    • 辅助请求      │              │
                     ├──────────────────┤              │
                     │ 4. 发送 HTTP 请求  │              │
                     │ 5. 接收响应        │              │
                     ├──────────────────┤              │
                     │ 6. [Post-response]│              │
                     │    • 提取变量      │──────────────┘
                     │    • 断言测试      │
                     │    • 可视化渲染    │
                     ├──────────────────┤
                     │ 7. 生成测试报告   │
                     │ 8. 更新历史记录   │
                     └──────────────────┘
```

### 6.6 日志输出格式

```javascript
// 脚本中使用
console.log("当前模块：", pm.info.moduleName);
console.info("Token 已刷新");
console.warn("响应时间较慢：", pm.response.responseTime + "ms");
console.error("状态码异常：", pm.response.code);
console.table([{name: '张三', age: 20}, {name: '李四', age: 25}]);
```

**控制台渲染效果**：
```
[14:32:05.123] [log]   当前模块：数字空间-残疾人服务一件事
[14:32:05.456] [info]  Token 已刷新
[14:32:05.789] [warn]  响应时间较慢：1245ms
[14:32:05.790] [error] 状态码异常：500
[14:32:05.791] [table] 
┌─────────┬─────┐
│  name   │ age │
├─────────┼─────┤
│  张三   │ 20  │
│  李四   │ 25  │
└─────────┴─────┘
```

### 6.7 安全沙箱规则

| 限制项 | 说明 |
|--------|------|
| **全局对象** | 仅暴露 `pm`, `console`, `setTimeout`, `setInterval`, `clearTimeout`, `clearInterval`, `Promise`, `JSON`, `Math`, `Date`, `Array`, `Object`, `String`, `Number`, `Boolean`, `RegExp`, `Error`, `Map`, `Set`, `WeakMap`, `WeakSet`, `Proxy`, `Reflect`, `Symbol`, `Intl`, `ArrayBuffer`, `Uint8Array`, `TextEncoder`, `TextDecoder`, `URL`, `URLSearchParams`, `atob`, `btoa` |
| **禁止访问** | `window`, `document`, `navigator`, `location`, `fetch`, `XMLHttpRequest`, `WebSocket`, `localStorage`, `indexedDB`, `eval`, `Function` |
| **网络限制** | 所有 HTTP 请求必须通过 `pm.sendRequest`，由主进程代理执行，受浏览器 CORS 策略约束 |
| **超时熔断** | 脚本执行超过 30 秒强制抛出 `ScriptTimeoutError` |
| **内存限制** | 单次脚本执行堆内存超过 128MB 强制终止 |
| **循环检测** | 死循环检测（单次事件循环超过 10 万次操作中断） |

---

## 七、三视场布局差异

### 7.1 视场总览

| 维度 | Popup 浮窗 | Side Panel 侧边栏 | 全屏页 |
|------|-----------|------------------|--------|
| **入口** | 点击工具栏图标 | 右键「打开侧边栏」/快捷键 | `chrome-extension://id/index.html` |
| **尺寸** | 宽 800px × 高 600px（Chrome 上限） | 宽 400-800px（用户可调），高 100vh | 全屏自适应 |
| **持久化** | 失焦自动关闭 | 跟随标签页，切换页面保持 | 独立标签页 |
| **定位** | 临时快速操作 | 边开发边调试 | 深度管理 |
| **导航深度** | 最多 2 级 | 完整树形 | 完整树形 + 右侧面板 |
| **编辑器** | 单行/简易 | 半高代码区 | 全功能 Monaco |
| **目标用户** | 查看/修改/快速发送 | 并行调试 | 复杂配置/批量测试 |

### 7.2 Popup 浮窗 —「闪电面板」

#### 布局结构（800 × 600）

```
┌─────────────────────────────────────────────────────────────┐
│  🔍 搜索...                                    [全屏展开 ⛶] │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  📂 最近访问模块（横向滚动卡片）                         │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │ │
│  │  │ 残疾人服│ │ 订单模块│ │ 用户中心│ │ + 更多  │      │ │
│  │  │ 务一件事│ │        │ │        │ │        │      │ │
│  │  │ [12接口]│ │ [8接口] │ │ [5接口] │ │        │      │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │ │
│  └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  ⚡ 快捷操作                                             │ │
│  │                                                         │ │
│  │  [发送上次请求]    [复制上次 cURL]    [查看历史]         │ │
│  │                                                         │ │
│  │  ── 或 ──                                               │ │
│  │                                                         │ │
│  │  快速发送（极简表单）                                    │ │
│  │  [GET ▼] [https://...              ] [发送]            │ │
│  │                                                         │ │
│  │  Body (JSON):                                           │ │
│  │  ┌─────────────────────────────────────────────────┐   │ │
│  │  │ { "key": "value" }                               │   │ │
│  │  └─────────────────────────────────────────────────┘   │ │
│  └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  🧪 最近历史（最近 5 条）                                │ │
│  │  🟢 GET  /api/user      200 OK  45ms  14:32             │ │
│  │  🟢 POST /api/login     200 OK  120ms 14:30             │ │
│  │  🔴 GET  /api/order     500 ERR 2.3s  14:28             │ │
│  │  🟡 GET  /api/slow      200 OK  3.1s  14:25             │ │
│  │  🟢 POST /api/submit    201 OK  89ms  14:20             │ │
│  └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  [⚙️ 设置]  [🌐 环境: 本地开发]  [📊 统计]        [打开完整版 →] │
└─────────────────────────────────────────────────────────────┘
```

#### 核心交互规则

| 场景 | 行为 |
|------|------|
| **模块卡片** | 点击直接进入该模块的「快速发送」模式，预填充 baseUrl |
| **搜索框** | 实时搜索跨模块的接口名/URL，回车直接发送第一条 |
| **快速发送** | 仅支持 JSON/Text Body，自动识别 Content-Type |
| **历史记录** | 点击直接复用该请求（打开 Side Panel 或全屏页填充） |
| **展开按钮** | 新开全屏标签页，并携带当前上下文（如正在编辑的请求） |
| **失焦关闭** | 点击外部区域浮窗关闭，但请求发送中的状态会 Toast 提示 |

#### 数据展示限制
- 不展示完整接口树（空间不足）
- 不展示响应 Body 的完整格式化（仅显示状态码/时间/大小 + 折叠的预览）
- 脚本编辑器不可用（仅显示「有前置/后置脚本」徽标）

### 7.3 Side Panel 侧边栏 —「并行调试台」

#### 布局结构（默认 480px 宽，可拖拽至 800px）

```
┌─────────────────────────────────────────────────────────────┐
│  [≡] 数字空间          [🔍] [⚙️] [⛶ 展开]                    │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌───────────────────────────────────────────┐ │
│  │          │ │  POST 数字空间-发起会话                    │ │
│  │ 📂 大类   │ │  ───────────────────────────────────────  │ │
│  │          │ │  [Params] [Body ●] [Headers] [Auth] [前置]│ │
│  │ ▼ 数字空间│ │                                           │ │
│  │   ▶ 残疾人│ │  {                                        │ │
│  │   ▶ 公钥同│ │    "systemId": "C90-44001697",           │ │
│  │   ▶ 会话管│ │    "query": {                             │ │
│  │          │ │      "svcCode": "bizUrRequest",            │ │
│  │ ▶ 电商系统│ │      "itemName": "残疾人服务一件事"         │ │
│  │          │ │    }                                       │ │
│  │ ▶ 内部工具│ │  }                                        │ │
│  │          │ │                                           │ │
│  ├──────────┤ │  [发送] [保存] [生成代码]                  │ │
│  │ ⚡ 快捷   │ │                                           │ │
│  │  请求     │ ├───────────────────────────────────────────┤ │
│  ├──────────┤ │                                           │ │
│  │ 🌐 环境   │ │  200 OK │ 652ms │ 918B                    │ │
│  │  本地开发 │ │  [Body] [Headers] [Cookie] [控制台]         │ │
│  ├──────────┤ │                                           │ │
│  │ 📜 历史   │ │  {                                        │ │
│  │  记录     │ │    "errcode": 0,                          │ │
│  └──────────┘ │    "data": {                               │ │
│               │      "realUrl": "https://..."               │ │
│               │    }                                        │ │
│               │  }                                          │ │
│               │                                             │ │
│               │  [复制] [搜索] [下载]                        │ │
│               └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 响应式断点

| 宽度范围 | 布局调整 |
|----------|----------|
| **300-400px** | 左侧导航完全折叠为图标栏（仅显示 emoji 图标，hover 出 Tooltip） |
| **400-600px** | 左侧导航显示图标 + 模块名（截断），接口树隐藏，通过下拉选择接口 |
| **600-800px** | 完整左侧树形导航，但请求/响应区上下堆叠（非左右分栏） |
| **>800px** | 请求/响应左右分栏（与全屏页一致） |

#### 浏览器特有交互

| 功能 | 实现 |
|------|------|
| **跟随当前页** | Side Panel 绑定到具体标签页，切换标签页时内容跟随（Chrome Side Panel API 特性） |
| **页面联动** | 在当前网页选中 JSON 文本 → 右键「发送到 Side Panel 格式化」→ 自动打开插件并粘贴到 Body |
| **Network 捕获** | 点击「捕获当前页请求」→ 列出当前标签页最近 10 条 Network 请求 → 一键导入 |
| **分屏拖拽** | 从 Side Panel 拖拽接口名到网页 → 生成并复制 cURL 命令 |

### 7.4 全屏页 —「指挥中心」

#### 布局结构（1920 × 1080 示例）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [≡]  API Fox Lite                              [🔍 全局搜索] [🌐 环境] [👤]  │
├──────────────────────────┬──────────────────────────────────┬───────────────┤
│                          │                                  │               │
│  📂 大类与模块导航        │      主工作区（请求编辑）         │   右侧面板    │
│  ─────────────────────     │      ─────────────────────         │   ─────────   │
│                          │                                  │               │
│  ▼ 数字空间               │  ┌────────────────────────────┐  │  🧩 模块设置   │
│    ▼ 残疾人服务一件事      │  │ POST  [URL 输入框        ] │  │  ───────────  │
│      ● 发起会话          │  │      [发送] [保存] [▼]      │  │  类型: 通用API │
│      ○ 查询进度          │  └────────────────────────────┘  │  数据源: 未绑定 │
│      ○ 提交材料          │                                  │               │
│    ▶ 公钥同步            │  [Params] [Body ●] [Headers]    │  🌍 当前环境   │
│    ▶ 会话管理            │  [Cookies] [Auth] [前置 ●] [后置]│  ───────────  │
│                          │  [设置]                           │  baseUrl:     │
│  ▶ 电商系统              │                                  │  localhost:8080│
│  ▶ 内部工具              │  ┌────────────────────────────┐  │  token: ***   │
│                          │  │  JSON 编辑器（Monaco）      │  │               │
│                          │  │  行号 语法高亮 折叠  minimap │  │  📋 动态值    │
│                          │  │                              │  │  ───────────  │
│                          │  │  {                           │  │  {{$timestamp}}│
│                          │  │    "systemId": "..."          │  │  {{$guid}}    │
│                          │  │  }                           │  │  {{$randomInt}}│
│                          │  └────────────────────────────┘  │               │
│                          │                                  │  🔖 接口信息   │
│                          │  [美化] [压缩] [动态值]           │  ───────────  │
│                          │                                  │  ID: req_xxx  │
│                          ├──────────────────────────────────┤  创建: 2024.. │
│                          │      下/右分栏（响应展示）        │  修改: 2024.. │
│                          │      ─────────────────────         │               │
│                          │                                  └───────────────┤
│                          │  200 │ 652ms │ 918B  [⬇️] [📋]                    │
│                          │                                  │
│                          │  [Body] [Headers] [Cookie] [控制台 ●] [实际请求]   │
│                          │                                  │
│                          │  ┌────────────────────────────┐  │
│                          │  │  JSON 树形查看器            │  │
│                          │  │  ▼ {                       │  │
│                          │  │    ▼ data                  │  │
│                          │  │      ▶ page: {total: 0}    │  │
│                          │  │      realUrl: "https://..."  │  │
│                          │  │    errcode: 0               │  │
│                          │  │  }                         │  │
│                          │  └────────────────────────────┘  │
│                          │                                  │
│                          │  [复制路径] [复制值] [对比差异]     │
│                          │                                  │
└──────────────────────────┴──────────────────────────────────┴───────────────┘
```

#### 三栏宽度规则

| 面板 | 默认宽度 | 可调范围 | 折叠行为 |
|------|----------|----------|----------|
| 左侧导航 | 260px | 200-400px | 可折叠为 48px 图标栏 |
| 主工作区 | 弹性填充 | 最小 600px | 不可折叠 |
| 右侧面板 | 300px | 0-400px | 可完全隐藏（快捷键 `Ctrl+Shift+R`） |

#### 全屏页专属功能

| 功能 | 说明 |
|------|------|
| **完整 Monaco** | 代码折叠、Minimap、多光标编辑、命令面板（F1）、Vim 模式 |
| **接口对比** | 选中两个接口 → 右键「对比差异」→ 并排显示参数/响应差异 |
| **批量测试** | 选中文件夹 → 「批量发送」→ 串行/并行执行，生成测试报告 |
| **文档模式** | 切换为「文档视图」→ 类似 Swagger UI 的只读展示 |
| **导入导出** | 支持拖拽文件到窗口导入；导出为多种格式 |
| **全局搜索** | `Ctrl+K` 调出命令面板，搜索跨所有模块的接口、变量、历史 |
| **主题切换** | Light / Dark / 跟随系统，独立记忆 |

### 7.5 三视场状态同步

```
┌─────────────────────────────────────────────────────────────┐
│                    中央状态管理 (Pinia)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Popup State│  │ Side Panel  │  │   Full Page State   │  │
│  │  (临时)     │  │  State      │  │   (持久)            │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘  │
│         │                │                    │             │
│         └────────────────┴────────────────────┘             │
│                      │                                      │
│              ┌───────┴───────┐                             │
│              │  chrome.storage │                             │
│              │  • 模块树结构  │                             │
│              │  • 环境变量    │                             │
│              │  • 历史记录    │                             │
│              │  • 用户设置    │                             │
│              └───────────────┘                             │
└─────────────────────────────────────────────────────────────┘
```

**同步规则**：
- **Popup → Side Panel**：点击「打开侧边栏」时，携带当前选中的模块 ID 和接口 ID
- **Popup → Full Page**：点击「展开」时，新开标签页并恢复当前编辑上下文
- **Side Panel ↔ Full Page**：双向实时同步（通过 `chrome.runtime` 消息广播），在一处修改变量，另一处立即更新
- **历史记录**：所有视场共享同一份历史，但在 Popup 中仅展示最近 5 条

### 7.6 视场切换快捷键

| 快捷键 | 行为 | 生效视场 |
|--------|------|----------|
| `Ctrl+Shift+A` | 打开/聚焦 Popup | 全局 |
| `Ctrl+Shift+S` | 打开/关闭 Side Panel | 全局 |
| `Ctrl+Shift+F` | 打开全屏页 | 全局 |
| `Esc` | 关闭 Popup / 收起 Side Panel | Popup / Side Panel |
| `Ctrl+Enter` | 发送请求 | 全部 |
| `Ctrl+S` | 保存接口 | Side Panel / Full Page |

---

## 八、数据存储方案

### 8.1 存储分层

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   本地浏览器     │ ←→ │   可选云端服务   │ ←→ │   团队共享仓库   │
│  (IndexedDB)    │     │  (GitHub Gist)  │     │  (私有服务器)   │
│                 │     │                 │     │                 │
│ • 模块变量本地值 │     │ • 模块变量远程值 │     │ • 完整模块备份  │
│ • 历史记录      │     │ • 接口定义      │     │ • 版本历史     │
│ • 个人设置      │     │ • 数据源配置    │     │ • 权限管理     │
└─────────────────┘     └─────────────────┘     └─────────────────┘

同步规则：
• 本地值永不自动上传，保护隐私
• 远程值修改后自动同步到云端（若已配置）
• 支持「导出模块为文件」手动备份
```

### 8.2 IndexedDB 结构（Dexie.js）

```typescript
import Dexie from 'dexie';

class ApiFoxDB extends Dexie {
  categories!: Dexie.Table<Category, string>;
  modules!: Dexie.Table<Module, string>;
  interfaces!: Dexie.Table<InterfaceNode, string>;
  history!: Dexie.Table<RequestHistory, string>;
  settings!: Dexie.Table<UserSettings, string>;

  constructor() {
    super('ApiFoxLite');
    this.version(1).stores({
      categories: 'id, sortOrder',
      modules: 'id, categoryId, name, [categoryId+sortOrder]',
      interfaces: 'id, moduleId, parentId, type, [moduleId+sortOrder]',
      history: 'id, timestamp, moduleId, interfaceId',
      settings: 'id'
    });
  }
}
```

---

## 九、技术实现建议

### 9.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **框架** | Vue 3 + TypeScript | 响应式、组件化成熟 |
| **构建** | Vite + CRXJS | 浏览器插件开发工具链 |
| **UI 库** | 自研组件 + Headless UI | 保持 Apifox 风格一致性 |
| **编辑器** | Monaco Editor | JSON/XML 编辑（全屏页）；CodeMirror 6（Popup 轻量版） |
| **HTTP 客户端** | 原生 `fetch` + `chrome.webRequest` API | 捕获 Cookie |
| **存储** | Dexie.js | IndexedDB 封装 |
| **状态管理** | Pinia | 跨视场状态同步 |

### 9.2 插件清单（Manifest V3）

```json
{
  "manifest_version": 3,
  "name": "API Fox Lite",
  "version": "1.0.0",
  "description": "轻量级 API 调试与管理浏览器插件",
  "permissions": [
    "storage",
    "activeTab",
    "sidePanel",
    "webRequest",
    "cookies"
  ],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_popup": "popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "32": "icons/icon32.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  "side_panel": {
    "default_path": "sidepanel.html"
  },
  "devtools_page": "devtools.html",
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],
  "icons": {
    "16": "icons/icon16.png",
    "32": "icons/icon32.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

---

## 十、开发阶段规划

| 阶段 | 目标 | 周期 | 交付物 |
|------|------|------|--------|
| **MVP** | Popup 快速请求 + 历史记录 + JSON 高亮响应 | 1-2 周 | 可发送请求的浮窗插件 |
| **V1.0** | Side Panel + 完整请求编辑 + 环境变量 + 导入导出 | 2-3 周 | 三视场完整运行 |
| **V1.5** | 大类/模块管理 + 文件夹树 + 前置/后置脚本 + 断言测试 | 2 周 | 可管理多项目 |
| **V2.0** | 数据源同步 + 云端备份 + 团队协作 + 文档生成 | 3-4 周 | 生产可用版本 |

---

## 附录：设计原则总结

1. **扁平化结构**：大类 → 模块二级结构，无工作空间概念，降低认知负担
2. **渐进式复杂度**：默认界面只展示常用功能，高级功能折叠在「更多」中
3. **上下文感知**：根据 Method 自动切换 Body 可用状态（GET 默认禁用 Body）
4. **零配置启动**：安装即用，自带示例项目（天气 API、JSONPlaceholder）
5. **浏览器原生体验**：自动同步浏览器 Cookie、复用系统代理、跟随浏览器主题
6. **三视场无缝切换**：Popup 快速操作 → Side Panel 并行调试 → 全屏页深度管理，状态实时同步

---

*文档结束*
